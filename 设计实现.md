# avc_genshin —— 设计与实现

> `CLAUDE.md` 的展开。CLAUDE.md 讲规则与速查;本文件讲**设计决策、可观测性绑定细则、avc API 手册、BGI 对照**。
> 进度看 `任务进度.md`;**代码是真相**(框架签名见 `src/framework/`,能力见 `src/abilities/`),本文不重复贴骨架。

## 1. 架构与设计决策

三层分工、核心原则、项目结构见 `CLAUDE.md §1/§2/§7`,不重复。下面只讲 CLAUDE.md 放不下的决策。

### 1.1 与 BetterGI 的核心区别（技术选型）

| 维度 | BetterGI | avc_genshin |
|---|---|---|
| 语言/形态 | C#/.NET 8/WPF | Python 3.10+,无 UI |
| 流程表达 | JSON 战斗脚本 + C# 状态机类 | **Python 代码即流程** |
| 调度 | `TaskTriggerDispatcher` 中央定时器轮询回调触发器 | **Runtime 跑 AI 脚本**;守护是自治协程循环,无中央调度 |
| 任务来源 | 开发者硬编码 + JS 脚本 | **AI 动态生成并添加** |
| 截图 | `Fischless.GameCapture` | avc `IScreenCapture` |
| YOLO/OCR/模板 | YoloSharp/PaddleOCR/OpenCvSharp | avc(onnxruntime/PP-OCRv6/OpenCV),**复用 BGI 的 ONNX 模型** |

**核心复用**：直接用 BetterGI 的 **ONNX 模型文件、JSON 路径数据、模板图片**，不重新训练。摒弃：JSON 编排、`StateMachineBase`、中央轮询。

### 1.2 执行引擎（Runtime）决策

- **同步外壳、异步内核**：AI 写同步代码;引擎内部 asyncio;主脚本跑工作线程,经同步桥调 loop。
- **单线程 loop**：所有 avc 调用都在 loop 线程,协作式调度,天然并发安全无需加锁。
- **守护 = 自己的 `while+sleep` 循环**（控制流在手），非 BGI 的"调度器每帧回调触发器"。
- **统一 `CancellationToken`**：取消/超时/异常 → 卸载所有守护 + 释放按键。
- **沙箱**：AST 白名单（禁 import/危险内置）+ 超时 + 注入 `ctx/g/工具`（即时任务用）。

> 实现见 `src/framework/runtime.py` + `cancellation.py` + `sandbox.py`;完整论述 `docs/design/01-执行引擎.md`。

### 1.3 可靠性地基（五件套）

| 组件 | 职责 | 实现 |
|---|---|---|
| `SceneEstimator` | 常驻守护,持续写 `SharedState.scene` | `framework/scene.py` + `shared.py` |
| `InputAuthority` | 输入通道权属（守护/任务租约,防抢键） | `framework/authority.py` |
| `FrameDaemon` | 单截图+推理,广播给订阅者（避免重复推理） | 守护池 |
| `Observe` | 时间线 + 失败分类 + 自动存证（**细则见 §2**） | `framework/observe.py` |
| `Policy` / `GuardRail` | 护栏：不花原石/不协作/区域/时预算;健康度滑窗触发 cancel | `framework/policy.py` + `guardrail.py` |

`g.*` 内部隐式接这些（持租约/读 scene/写 Observe/过 Policy），**任务作者无感**。详见 `docs/design/02-可靠性三件套.md`。

### 1.4 AI 工作流

```
意图 → AI 规划(读注册表,分解为已有任务+缺失) → 生成/复用 → 注册 → Runtime 执行(纯Python) → 失败截图+日志回流 → AI 修正 → 重新注册
```

LLM 只在**规划/生成/修正**介入;**执行**是纯 Python（成本/延迟约束）。AI 协作层（阶段 E）已**延后**,实机全过再开（见 `任务进度.md`）。

---

## 2. 能力可观测性（绑定细则）

> 被 `CLAUDE.md §8`、`observe.py` / `report.py` docstring 引用的权威章。所有 ability 与 `src/tasks/*.py`（含 AI 生成插件）**必须**遵守。

**动机**：`src/tasks/` 联合任务离线单测全过,一上实机全错,而 ability 大多静默——AI 没法坐到屏幕前,必须从输出按 `ability` 分组定位坏在哪个能力的哪个 stage/reason。这是**绑定原则**：每个 ability 与任务插件在「看 / 选 / 结果」决策点发事件。

**写端**（`framework/observe.py`，`ctx.observe` 永可调用;无 runtime 时返 `_NullObserve` no-op 单例,调用方永不判空）：

```python
ctx.observe.event("tp.resolve", ability="tp", phase="decide",
                  target=..., selected=..., ok=..., reason="name_not_found")
ctx.observe.event("nav.step", ability="nav", phase="observe",
                  pos=..., dist=..., heading_diff=..., throttle_key="nav.step")
```

**节流门（opt-in，防热轮询爆 JSONL）**——事件传以下任一标志才启用,否则全量落地：

| 模式 | 行为 | 用途 |
|---|---|---|
| `throttle_key="..."` | time-window 1s;同 key 成功/观测折叠,`ok=False` **永不节流** | `nav.step`/`pos.match` 轮询 |
| `_quiet=True` | 同 key 整 run 只发首条（任意 ok） | 场景分类器 10Hz、`g.wait_*` 5Hz |
| `_transition=True` | `ok` 跳变才发 | `survival.low_hp` False↔True |

> **优先级**：`_quiet`/`_transition` 显式模式 > 「`ok=False` 永不节流」> time-window。即 10Hz 的「未找到」`ok=False` 会被 `_quiet` 折叠（否则爆），但 time-window 模式下一次 `nav.step` 卡死值得每次即时浮现。窗口内折叠计数进下条 `elided` 字段。

**事件 schema 约定**：

| 字段 | 规则 |
|---|---|
| `kind` | `"<domain>.<stage>"`，domain 取自固定集：`tp/nav/pos/cam/trap/path/fight/survive/detect/reward/panel/task/run`（例 `tp.resolve`/`nav.step`/`survival.check`/`detect.template`） |
| `ability` | **短模块名**（tp/nav/pos/fighter/vision_utils/game_state/reward/tp_panel/path/trap/cam，或任务名）—— **AI 按此分组点名嫌疑**。必填 |
| `phase` | `detect|decide|act|observe` —— 让 AI 说「全 detect 过、decide 挂了」 |
| `ok` | `True|False`;纯观测省略;多态结果（`panel=TELEPORT/MARKER`、`pos.mode=local/global`）用专门字段,`ok` 只在真有成败时设 |
| `reason` | **稳定 slug**（非自由文本）：`timeout`/`abort_stuck`/`abort_too_far`/`pos_fail`/`pin_blocking`/`sift_no_match`/`heading_none`/`exhausted`… 自由文本进 `detail` |
| `attempt`/`max_attempts` | 重试循环（`tp.navigate` 迭代、`rotate_to`、`_use_burst`、reward 点击） |
| `source` | 多来源检测必填：`survival.check` 的 `source="shared"|"pixel"` |
| `throttle_key`/`sampled`/`elided` | 节流时出现 |
| `evidence` | `ok=False` 时存证路径（`Observe.save_evidence`） |
| 自动注入 | `ts`/`run_id`/`scene`/`level`（调用方不设） |

**读端**（`framework/report.py`，与 observe.py 分离）：

- `live_line(event)->str`：一条事件 → 一行 stderr 实时打印（runtime 订阅者）：`[tp] tp.resolve 失败 reason=name_not_found`。
- `summarize(timeline)->dict`：按 `ability` 分组 → `{ok, fail, failures:[{stage,reason,attempt,evidence}]}`。
- `summary_text(summary)->str`：teardown 打印,只列**有失败的 ability**。

runtime 在 `_teardown` 发 `run_summary` 事件（带 `summarize(timeline)`）+ stderr 打印 `summary_text`。AI 读 `logs/<run_id>.jsonl` 的 `run_summary` 行 → 按 `ability` 分组 → 配用户提示点名。

**AI 定位规则**：筛 `ok==False` 或检测 0/异常 → 该事件 `ability` 即嫌疑;看 `reason` slug 定分支;看 `phase` 定是检测/决策/动作哪段挂。

**避免重复**：只读探针（`find_*`/`has_*`/`wait_*`/`is_*`）只在咽喉发 `detect.*`，`g.*` 不另发 `action`;任务级 `<task>.step` 是粗粒度进度面包屑,细节由 ability 事件承载。

---

## 3. avc Python API 完整速查

> 最常用版见 `CLAUDE.md §5`。下面是完整签名;**代码是真相**（`src/framework/context.py` 的包装），以代码为准。

### 3.1 模块导入

```python
from avc import Input, Vision, Image
from avc._core import KeyCode, MouseButton, TemplateMatchMethod, MatchOrderBy
```

### 3.2 IScreenCapture（截图）

```python
sc = Input.createScreenCapture()
sc.setWindow("原神")              # 子串匹配窗口标题
sc.activateWindow("原神")         # 激活并前置
sc.refresh(); buf = sc.getBuffer()   # IImageBuffer（借用,生命周期归 sc;格式见 buf.imageType）
pos = sc.toScreen(bufX, bufY)        # 截图坐标→屏幕坐标 vec2i(.x/.y)
cropped = sc.crop(x, y, w, h)        # 返回新 IScreenCapture
```

### 3.3 IInputController（操作）

```python
ic = Input.createInputController()
ic.click(x, y); ic.click(x, y, MouseButton.right); ic.doubleClick(x, y)
ic.drag(x1,y1,x2,y2); ic.scroll(dx, dy)
ic.press(KeyCode.e); ic.press(KeyCode.e, holdMs=500)
ic.hotkey(KeyCode.ctrl, KeyCode.c); ic.typeText("你好")
ic.setMoveDurationMs(200); ic.setMoveSteps(20); ic.setKeyDelayMs(50)
# ⚠️ 拟人化:avc 绑定【无】setHumanize（实测仅 setMoveDurationMs/setMoveSteps/setKeyDelayMs）。
#    坐标抖动+0.8–1.2×随机间隔+按住时长随机由框架层实现（framework/utils.py + GameContext/g.* 套用）。
# 常用 KeyCode:w/a/s/d 移动, e/q 技能, f 交互, space 跳, esc 菜单, tab 队伍, m 地图, j 任务, num1~num4 切人, shift 冲刺
```

### 3.4 ITemplateMatcher（模板匹配）

```python
tm = Vision.createTemplateMatcher()
tm.setMethod(TemplateMatchMethod.ccoeffNormed)   # 默认,光照鲁棒
tm.setGreenMask(True)           # 纯绿(0,255,0)当透明
tm.setNmsIoU(0.2); tm.setOrderBy(MatchOrderBy.horizontal); tm.setRoi(x,y,w,h)
idx = tm.addTemplatePath("templates/x.png", 0.8)   # 阈值 0.8
n = tm.match(buf); r = tm.getMatch(0)   # 命中数 / MatchResult(.x/.y/.w/.h/.score/.templateIndex)
```

### 3.5 ITextRecognizer（OCR，PP-OCRv6）

```python
ocr = Vision.createTextRecognizer()
ocr.setThreshold(0.3); ocr.setRoi(x,y,w,h); ocr.setUseGpu(True)
n = ocr.recognize(buf)        # det+rec,返回文字框数
text, r = ocr.getMatch(0)     # (str, OcrResult(.x/.y/.w/.h/.score))
```

### 3.6 IImageBuffer（图像）

```python
buf.width; buf.height; buf.imageType       # 格式由 imageType 决定（ImageType 枚举：bgra8/rgba8/rgb8/bgr8/r8/…）
raw = buf.to_bytes()          # → bytes,通道序/布局由 imageType 决定
cropped = Image.crop(buf, x, y, w, h); resized = Image.resize(buf, 640, 640)
buf.save("out.png")
```

---

## 4. 领域能力（L2）

### 4.1 识别

| 目标 | 方式 | 实现 |
|---|---|---|
| 敌人/血条/采集物 | YOLO | `GenshinDetector` + `bgi_world.onnx` |
| 角色/技能CD | 分类 | `avatar_side_classify_sim.onnx` / `q_classify_sim.onnx` |
| UI 按钮/图标 | 模板匹配 | avc `ITemplateMatcher` |
| 文字/状态 | OCR | avc `ITextRecognizer` |
| 玩家位置 | 特征 | 小地图匹配全地图（`src/abilities/navigation/position.py`） |

**`GenshinDetector`**（见 `src/abilities/detector.py`）：推理+解码**全走 avc `IYoloDetector`**（C++，忠实移植标准 Ultralytics 流程），本层只做 `Box→Detection` 包装 + 从 ONNX 元数据读类名（`getClassName`；旧 `label.json` 废弃，类名**带空格**如 `"enemy identify"`）。**avc 是硬依赖**，不回退 ort/cv2。avc 内部解码 3 坑（别重踩）：输出 `[1,4+nc,N]` 需转置 → **不做 sigmoid**（已 bake 进图）→ letterbox 反变换 `(x-pad)/scale` → 按类纯 IoU NMS（0.45）。

### 4.2 导航

复用 BetterGI JSON 路径数据。链路：小地图定位（`position.py`）→ 朝目标旋转视角（`camera.py`，鼠标拖拽）→ WASD 移动（`path_executor.py`）→ 卡住检测/脱出（`trap_escaper.py`）→ 到点 action。action 类型：`move/combat/collect/mine/teleport/npc`。详见 `src/abilities/navigation/`。

### 4.3 战斗（SimpleFighter）

简化版：固定连招（E→Q→普攻×N）+ `has_enemy()` 循环 + Q 可用检测 + 按槽位切人 + 生存（红血吃药/换人、死亡传送最近神像复活）。首期够用秘境/地脉花。见 `src/abilities/fighter/`。

---

## 5. BetterGI 对照与复用

### 5.1 功能模块映射（BetterGI → avc_genshin）

| BetterGI 功能 | 形态 | 要点 |
|---|---|---|
| AutoPick / 自动拾取 | **守护** | YOLO 检测交互物 → 按 F（白/黑名单） |
| AutoSkip / 自动剧情 | **守护** | 模板匹配跳过键 + OCR 选橙选项 |
| QuickTeleport / 快速传送 | **守护** | 地图选点后自动确认 |
| AutoEat / 自动吃药 | **守护** | 红血吃营养袋 / 复活 |
| GameLoading / 加载等待 | **守护** | 检测加载界面,阻塞主流程 |
| AutoDomain / 秘境 | **任务** | 钥匙→战斗→古树→领奖→退出 |
| AutoBoss / 首领 | **任务** | 传送→讨伐→战斗→领奖→补树脂 |
| AutoLeyLine / 地脉花 | **任务** | 导航 + 战斗 + 领奖 |
| AutoPathing / 采集挖矿 | **任务** | 导航 + 到点 action |
| AutoFight | **能力** | `SimpleFighter` |
| 一条龙 | **任务组合** | AI 用 `ctx.run()` 组合 |

### 5.2 复用资源（经 `res.*` 定位器，见 `docs/design/03 §5`）

| 资源 | 来源 | 用途 |
|---|---|---|
| `bgi_world/fish/mine/tree.onnx` 等 | BGI Release `Assets/Model/` | YOLO → `resources/models/` |
| `avatar_side/q_classify/avatar.onnx` | 同上 | 角色/技能识别 |
| 路径 JSON | BGI 社区 | `PathExecutor` → `resources/paths/` |
| 模板图片 | 自截 / BGI | 模板匹配 → `resources/templates/` |
| 全地图数据 | BGI Release（300M+） | 小地图定位 → `resources/map/`（gitignore） |

### 5.3 BetterGI 源码索引（参考）

| 功能 | BetterGI 路径 | 说明 |
|---|---|---|
| YOLO 推理/解码 | `Core/Recognition/ONNX/BgiYoloPredictor.cs` + YoloSharp | 标准 Ultralytics;`Predictor.cs` 已 `[Obsolete]` 空壳 |
| 模型注册 | `Core/Recognition/ONNX/BgiOnnxModel.cs` | 所有模型路径 |
| OCR / 模板 / 截图 / 操作 | `Core/Recognition/{OCR,OpenCv}`、`Fischless.{GameCapture,WindowsInput}` | 对应 avc 各能力 |
| 路径执行 / 相机 / 脱出 | `GameTask/AutoPathing/{PathExecutor,CameraRotateTask,TrapEscaper}.cs` | 导航核心 |
| 小地图定位 | `GameTask/Common/Map/MiniMap/` | 特征匹配 |
| 执行引擎 / 战斗 / 秘境 | `GameTask/{TaskTriggerDispatcher,AutoFight,AutoDomain}` | 对照（我们用 Runtime / `ctx.run` 替代） |

> BetterGI 整体架构分析见 `docs/bgi-framework/00-主体框架.md`。
