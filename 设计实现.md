# avc_genshin —— 完整设计与实现

> 本文件是 `CLAUDE.md` 的展开。新会话读这两份即可上手开发。
> 框架设计的深入展开见 `docs/design/`（`00-框架总纲.md` / `01-执行引擎.md`）。

---

## 1. 项目定位与三层架构

avc_genshin 是一个**以 AI 动态添加任务为核心**的原神自动化框架。AI 生成 Python 任务脚本，框架加载运行，通过 CV + 模拟输入操作游戏。

### 1.1 三层分工

| 层 | 仓库 | 职责 | 关键内容 |
|---|---|---|---|
| **L1 avc** | `D:/Work/github/avc`（C++） | 通用 CV+IO 能力 | `IScreenCapture`、`IInputController`（含拟人化）、`ITemplateMatcher`、`ITextRecognizer`、ONNX。SWIG→Python `avc` 模块 |
| **L2 avc_genshin** | 本仓库（Python） | 游戏领域框架 | 任务体系、Runtime、守护任务库、高层 API、导航/战斗/检测等领域能力、AI 协作层 |
| **L3 AI 任务插件** | `tasks/*.py` | 具体业务 | `daily_quest`、`farm_domain`、`collect_flowers`… 由 AI 动态生成并添加 |

### 1.2 与 BetterGI 的核心区别

| 维度 | BetterGI | avc_genshin |
|---|---|---|
| 语言 | C#/.NET 8/WPF | Python 3.10+，无 UI |
| 流程表达 | JSON 战斗脚本 + C# 状态机类 | **Python 代码即流程** |
| 调度 | `TaskTriggerDispatcher` 中央定时器轮询回调触发器 | **Runtime 运行 AI 脚本**；守护任务是自治协程循环，无中央调度器 |
| 任务来源 | 开发者硬编码 + JS 脚本 | **AI 动态生成并添加**（任务是一等公民） |
| 截图 | `Fischless.GameCapture`（BitBlt/DwmSharedSurface/GraphicsCapture） | avc `IScreenCapture`（~5ms） |
| YOLO | YoloSharp + OnnxRuntime | onnxruntime Python（复用 BetterGI ONNX 模型） |
| OCR | PaddleOCR ONNX | avc `ITextRecognizer`（PP-OCRv6） |
| 模板匹配 | OpenCvSharp | avc `ITemplateMatcher`（OpenCV+NMS） |
| 操作 | PostMessage/SendInput | avc `IInputController`（SendInput + 拟人化） |
| 路径 | AutoPathing（JSON+执行器） | 复用 BetterGI JSON 路径 + Python 执行器 |
| 战斗 | 完整（角色识别+CD+脚本引擎） | 简化版（固定连招 + Q 可用检测） |

**核心复用**：直接使用 BetterGI 的 **ONNX 模型文件、JSON 路径数据、模板图片**，不重新训练。

---

## 2. 核心设计原则

1. **Python 代码即流程** —— 任务流程用纯 Python（`if/while/for/await`）。摒弃 JSON 流程定义、状态机类、中央轮询调度器。`genshin_daily_design.md` 所说"无框架、无状态机、无轮询"即指此（流程层）；基础设施 Runtime 是要的。
2. **AI 动态添加任务** —— 任务是一等公民，写 `tasks/*.py` 即插件，热加载，注册后即可按名调用、可组合。框架为"AI 添加任务"而设计。
3. **基础设施不侵入流程** —— Runtime 负责"安全地跑你写的 Python"，不规定流程怎么走。
4. **同步外壳、异步内核** —— AI 写同步代码；引擎内部 asyncio；单线程 loop 天然解决 avc 并发安全。

---

## 3. 框架设计概览

> 完整论述见 `docs/design/00-框架总纲.md` 与 `01-执行引擎.md`。此处为浓缩。

### 3.1 执行引擎（Runtime）

```
┌──────────── asyncio event loop（单线程，所有 avc 调用在此）────────────┐
│   守护任务池：auto_pick / auto_skip / loading_wait / auto_eat / …     │
│        （各自 while+sleep 循环，协作式并发，控制流在自己手里）          │
└──────────────────────────────────────────────────────────────────────┘
        ▲ run_coroutine_threadsafe                    ▲ create_task
        │                                              │
┌───────┴────────┐                         ┌──────────┴─────────────┐
│ 工作线程(主脚本)│  g.* 同步桥              │ mount/unmount(脚本调用) │
│ AI 同步代码     │─────────────────────────▶│                        │
│ task_xxx(ctx,g) │                         │                        │
└────────────────┘                         └────────────────────────┘
        共享：GameContext(sc/ic/tm/ocr) · RunContext · CancellationToken
```

- **主脚本**（对应 BetterGI `ISoloTask`）：AI 生成的同步流程，跑在工作线程，经同步桥调 loop。
- **守护任务**（对应 BetterGI `ITaskTrigger`）：自治 async 循环，自己管频率，UI 判断写在自己内部；**非**被调度器回调。
- **CancellationToken**：统一取消（Ctrl+C/超时/异常）→ 卸载所有守护 + 释放按键。
- **沙箱**：AST 白名单（禁 import/危险内置）+ 超时 + 注入 `ctx/g/工具`。

### 3.2 任务体系

- **任务契约**：`@task(name/desc/daemons/requires/params)` + `def main(ctx, g, **params)`。
- **注册表 + 热加载**：启动扫描 `tasks/`，运行时改文件即重载；AI 加任务 = 写一个符合契约的 `.py`。
- **即时 vs 持久**：intent 现场生成 → 缓存 `cache/tasks/<hash>.py`；成熟的可提升为持久任务。
- **组合**：`ctx.run(name, **params)`（纯 Python 调用，非配置编排）。

### 3.3 AI 全程处理工作流

```
意图 → AI 规划(读注册表,分解为已有任务+缺失) → 生成/复用 → 注册 → Runtime 执行(纯Python) → 失败截图+日志回流 → AI 修正 → 重新注册
```

LLM 只在**规划/生成/修正**区介入；**执行**区是纯 Python（成本/延迟约束）。

---

## 4. 框架基础设施（代码骨架）

### 4.1 GameContext —— avc 实例管理

```python
class GameContext:
    def __init__(self, window_title="原神"):
        self.sc = Input.createScreenCapture()
        self.ic = Input.createInputController()
        self.tm = Vision.createTemplateMatcher()
        self.ocr = Vision.createTextRecognizer()
        self.sc.setWindow(window_title); self.sc.activateWindow(window_title)
        self.ic.setMoveDurationMs(200); self.ic.setMoveSteps(20)   # 拟人化由框架层做（avc 无 setHumanize，见 §5.3）

    def capture(self): self.sc.refresh(); return self.sc.getBuffer()
    def click_at(self, bx, by): p = self.sc.toScreen(bx, by); self.ic.click(p.x, p.y)
    def click_center(self, r): self.click_at(r.x + r.w//2, r.y + r.h//2)
    def press(self, key, holdMs=0): self.ic.press(key, holdMs)
```

### 4.2 Runtime / 守护 / 高层 API（签名）

```python
class Runtime:           # 引擎入口（单例）
    def run_intent(self, user_intent, timeout=600) -> bool: ...   # intent→缓存→校验→执行
    def run_task(self, name, **params) -> bool: ...               # 跑已注册任务
    def run_code(self, code, timeout=600) -> bool: ...            # 沙箱执行
    def mount(self, name, **opts): ...                            # 挂载守护
    def unmount(self, name): ...

class Daemon(Protocol):
    name: str
    async def run(self, ctx, token) -> None: ...                  # 自治 while+sleep 循环

class HighLevelApi:      # g.*
    def teleport_to(self, name): ...   # 同步外壳,内部桥接到 loop
    def go_to(self, pos): ...
    def talk(self, option): ...
    def wait_text(self, kw, timeout=10): ...
    def decide(self, question, schema, *, context="auto", timeout=30): ...  # ⏳预留：运行时决策层（后置），见 docs/design/01-执行引擎.md §11
    # ... 见 CLAUDE.md §6

class TaskRegistry:      # 任务注册表
    def register(self, task_or_path): ...
    def get(self, name) -> TaskDescriptor: ...
    def list(self) -> list: ...          # 供 AI 规划器枚举
    def reload(self, name): ...          # 热加载
```

### 4.3 可靠性地基（签名，详见 `docs/design/02-可靠性三件套.md`）

```python
class SceneEstimatorDaemon(Daemon):       # §1 常驻守护，持续写 SharedState.scene
    name = "scene_estimator"
    async def run(self, ctx, token): ...

class InputAuthority:                      # §2 输入通道权属
    def acquire(self, channels, holder, priority=0) -> "InputLease": ...
    def release(self, lease): ...

@daemon(name="auto_pick", owns_keys={InputChannel.INTERACT},
        scenes=[Scene.MAIN_UI, Scene.DOMAIN], priority=0)
class _DaemonExample(Daemon): ...          # 守护声明通道 + 活跃场景

class FrameDaemon(Daemon):                 # §3 单一截图+推理，广播给订阅者
    name = "frame"

class Observe:                             # §4 时间线 + 失败分类 + 自动存证
    def event(self, kind, **fields): ...
    def timeline(self) -> list: ...

@dataclass
class Policy:                              # §5 护栏
    never_spend: list; no_coop: bool; regions: list; time_budget_s: int; confirm_threshold: str
```

> `g.*` 内部隐式接这些：`go_to` 持有 `MOVE`/`MOUSE_MOVE` 租约、读 `scene`、写 `Observe`、过 `Policy`——**任务作者无感**。

---

## 5. avc Python API 完整速查

### 5.1 模块导入

```python
from avc import Input, Vision, Image
from avc.Input import (IInputController, IScreenCapture,
                       createInputController, createScreenCapture)
from avc.Vision import (ITemplateMatcher, ITextRecognizer,
                        createTemplateMatcher, createTextRecognizer,
                        findWindowByName, getActiveWindow, getWindowName)
from avc.Image import IImageBuffer, loadImage, crop, resize, toBytes
from avc._core import KeyCode, MouseButton, ImageType, TemplateMatchMethod, MatchOrderBy
```

### 5.2 IScreenCapture（截图）

```python
sc = createScreenCapture()
sc.setWindow("原神")              # 子串匹配窗口标题
sc.activateWindow("原神")         # 激活并前置
sc.refresh(); buf = sc.getBuffer()   # IImageBuffer（借用，生命周期归 sc）
pos = sc.toScreen(bufX, bufY)        # 截图坐标→屏幕坐标，返回 vec2i(.x/.y)
cropped = sc.crop(x, y, w, h)        # 返回新 IScreenCapture
sc.save("debug.png")
sc.width(); sc.height()
```

### 5.3 IInputController（操作）

```python
ic = createInputController()
ic.moveTo(x, y); ic.moveBy(dx, dy)
ic.click(x, y); ic.click(x, y, MouseButton.right); ic.doubleClick(x, y)
ic.drag(x1, y1, x2, y2); ic.scroll(dx, dy); ic.getCursorPos()
ic.keyDown(KeyCode.w); ic.keyUp(KeyCode.w)
ic.press(KeyCode.e); ic.press(KeyCode.e, holdMs=500)
ic.hotkey(KeyCode.ctrl, KeyCode.c); ic.typeText("你好")
ic.setMoveDurationMs(200); ic.setMoveSteps(20); ic.setKeyDelayMs(50)
# ⚠️ 拟人化：avc 绑定【无】setHumanize/setJitterSeed/setClickHoldMs（实测仅有
#    setMoveDurationMs/setMoveSteps/setKeyDelayMs）。拟人化（坐标抖动 + 0.8–1.2×
#    随机间隔 + 按住时长随机）由框架层实现：framework/utils.py 原语 + GameContext/高层 API 套用。
# 常用 KeyCode：w/a/s/d 移动, e/q 技能, f 交互, space 跳, esc 菜单,
#              tab 队伍, m 地图, j 任务, num1~num4 切人, shift 冲刺
```

> ⚠️ 实测（2026-08）：avc 绑定**无** `setHumanize`/`setJitterSeed`/`setClickHoldMs`（拟人化改为框架层，见上）；`activateWindow` **已存在**（`sc.activateWindow(title)`，已非待补）。

### 5.4 ITemplateMatcher（模板匹配）

```python
tm = createTemplateMatcher()
tm.setMethod(TemplateMatchMethod.ccoeffNormed)   # 默认，光照鲁棒
tm.setGreenMask(True)           # 纯绿(0,255,0)当透明
tm.setNmsIoU(0.2); tm.setOrderBy(MatchOrderBy.horizontal)
tm.setRoi(x, y, w, h)
idx = tm.addTemplatePath("templates/x.png", 0.8)   # 阈值 0.8
idx = tm.addTemplate(imageBuffer, 0.7)
tm.clearTemplates()
n = tm.match(sceneBuffer)        # 返回命中数
r = tm.getMatch(0)               # MatchResult(.x/.y/.w/.h/.score/.templateIndex)
center = tm.matchCenter(r)       # vec2i
```

### 5.5 ITextRecognizer（OCR，PP-OCRv6）

```python
ocr = createTextRecognizer()
ocr.setThreshold(0.3); ocr.setRoi(x, y, w, h); ocr.setUseGpu(True)
n = ocr.recognize(sceneBuffer)   # det+rec，返回文字框数
text, r = ocr.getMatch(0)        # (str, OcrResult(.x/.y/.w/.h/.score))
center = ocr.ocrCenter(r)
```

### 5.6 IImageBuffer（图像）

```python
buf = loadImage("templates/btn.png")
buf.width; buf.height; buf.imageType
raw = buf.to_bytes()             # → bytes (BGRA8)，喂 YOLO
buf.from_bytes(data)
cropped = crop(buf, x, y, w, h); resized = resize(buf, 640, 640)
buf.save("out.png"); buf.to_base64()
```

---

## 6. 领域能力（L2）

### 6.1 识别

| 目标 | 方式 | 实现 |
|---|---|---|
| 敌人/血条/采集物 | YOLO | `GenshinDetector` + `bgi_world.onnx`（onnxruntime） |
| 矿物 | YOLO | `bgi_mine.onnx` |
| 钓鱼 | YOLO | `bgi_fish.onnx` |
| 秘境古树 | YOLO | `bgi_tree.onnx` |
| 角色/技能CD | 分类 | `avatar_side_classify_sim.onnx` / `q_classify_sim.onnx` |
| UI 按钮/图标 | 模板匹配 | avc `ITemplateMatcher` |
| 文字/状态 | OCR | avc `ITextRecognizer` |
| 玩家位置 | 模板/特征 | 小地图匹配全地图（见 §6.3） |

**`GenshinDetector` 要点**（源码核实 YoloSharp = 标准 Ultralytics 格式，见 `src/abilities/detector.py`）：输入 BGRA8 bytes → BGRA→RGB → letterbox(min 比例 + 居中 padding) 到 640 → NCHW float32 /255 → onnxruntime 推理 → 输出 `[1,4+nc,N]` 转置 → **不做 sigmoid**（已 bake 进图）→ cx,cy,w,h 转 xyxy → `(x-pad)/scale` 反 letterbox → 按类纯 IoU NMS(0.45) → `{cls: [Detection(x1,y1,x2,y2,score)]}`。类名从 ONNX 元数据 `names` 读（旧 `label.json` 已废弃）。

### 6.2 操作

经 `GameContext` / `g.*`。**拟人化必须启用**；坐标基于 1080p，`sc.toScreen()` 转屏幕坐标。

### 6.3 导航（PathExecutor）

复用 BetterGI JSON 路径数据（社区大量积累）。执行器：小地图定位玩家位置 → 朝目标旋转视角（鼠标拖拽）→ WASD 移动 → 卡住检测/脱出（参考 BGI `TrapEscaper`）→ 到点执行 action。

```python
class PathExecutor:
    def execute(self, path_data): ...        # 遍历 waypoints
    def _navigate_to(self, target_pos): ...  # 定位+转向+移动循环
    def _get_player_position(self): ...      # 小地图模板匹配全地图
    def _rotate_camera_to(self, dx, dy): ...
    def load_path(self, name): ...
```

action 类型：`move / combat / collect / mine / teleport / npc`。

### 6.4 战斗（SimpleFighter）

简化版：固定连招（E→Q→普攻×N）+ `has_enemy()` 循环 + Q 可用检测 + 按槽位切人。首期够用秘境/地脉花；后续可增强（参考 BGI `AutoFight`）。

```python
class SimpleFighter:
    def fight_until_clear(self, timeout=120): ...
    def switch_character(self, slot): ...
```

---

## 7. 功能模块映射（BetterGI → avc_genshin）

| BetterGI 功能 | 形态 | 实现要点 |
|---|---|---|
| AutoPick 自动拾取 | **守护** | YOLO 检测交互物 → 按 F（白/黑名单） |
| AutoSkip 自动剧情 | **守护** | 模板匹配跳过键 + OCR 选橙色选项/关书页 |
| QuickTeleport 快速传送 | **守护** | 地图选点后自动确认传送 |
| AutoEat 自动吃药 | **守护** | 红血吃营养袋 / 复活 |
| GameLoading 加载等待 | **守护** | 检测加载界面，阻塞主流程 |
| AutoDomain 自动秘境 | **任务** | 开钥匙→战斗→古树→领奖→退出 循环 |
| AutoBoss 首领讨伐 | **任务** | 传送→讨伐→战斗→领奖→补树脂 |
| AutoWood 自动伐木 | **任务** | 按 Z + 上下线刷新 |
| AutoFishing 自动钓鱼 | **任务** | YOLO 抛竿/收杆 + 进度小游戏 |
| AutoLeyLine 地脉花 | **任务** | 导航 + 战斗 + 领奖 |
| AutoPathing 采集/挖矿 | **任务** | 导航 + 到点 action |
| AutoFight 战斗 | **能力** | `SimpleFighter` + 可选战斗守护 |
| 一条龙 | **任务组合** | AI 用 `ctx.run()` 组合上述任务 |

---

## 8. 复用 BetterGI 资源

| 资源 | 来源 | 用途（经 `res.*` 定位器访问，见 `docs/design/03-项目结构与公共层.md §5`） |
|---|---|---|
| `bgi_world/fish/mine/tree.onnx` 等 | BetterGI Release `Assets/Model/` | YOLO 检测 → `resources/models/` |
| `avatar_side/q_classify/avatar.onnx` | 同上 | 角色/技能识别 → `resources/models/` |
| `label.json` | 同上 | 类别标签 → `resources/models/` |
| 路径 JSON | BetterGI 社区 | `PathExecutor` 执行 → `resources/paths/` |
| 模板图片 | 自截 / BetterGI | 模板匹配 → `resources/templates/`（按功能分子目录） |
| 全地图数据 | BetterGI Release（300M+） | 小地图定位 → `resources/map/`（gitignore） |

---

## 9. 项目结构（src layout，详见 `docs/design/03-项目结构与公共层.md`）

```
avc_genshin/
├── src/                    所有代码（三个顶层包）
│   ├── framework/          框架核心+公共层（import: from framework import task）
│   │                       runtime/task/registry/sandbox/cancellation/high_level_api
│   │                       + context/config/resources/errors/utils（公共层）
│   │                       + shared/scene/authority/frame/observe/logging/policy/planner/daemons
│   ├── abilities/          领域能力：navigation/fighter/detector/vision_utils/game_state
│   └── tasks/              L3 持久任务插件（AI 写入；内置示例进 git，动态任务见 03 §4）
├── resources/              游戏资源：templates/ paths/ models/ ocr/ map/（大文件 gitignore）
├── cache/ logs/ debug/     即时任务缓存(cache/tasks/) / 结构化日志(JSONL) / 失败存证
├── main.py                 薄入口壳（也支持 python -m framework）
├── pyproject.toml          src layout（pip install -e .；打包 framework+abilities）
└── docs/ CLAUDE.md 设计实现.md
```

> 四类分离：**代码**(src/) · **插件**(tasks/) · **资源**(resources/) · **产物**(cache/logs/debug)。资源定位器 `res.path()` 统一访问 + `BGI_ROOT` 复用；结构化日志见 03 §7。

---

## 10. 开发计划

| 阶段 | 内容 | 优先级 | 依赖 |
|---|---|---|---|
| 一 | 基础链路：GameContext + GenshinDetector + 截图→推理原型 | P0 | avc Python SDK |
| 二 | 操作闭环：补 SWIG 绑定（拟人化/激活窗口）+ 坐标转换 | P0 | 阶段一 |
| 三 | Runtime + 守护 + **可靠性地基**：执行引擎 + auto_pick/skip/loading 守护 + SceneEstimator/InputAuthority/Observe/Policy（见 `docs/design/02-可靠性三件套.md`） | P0 | 阶段二 |
| 四 | 任务体系：`@task` 契约 + Registry + 热加载 + 沙箱 | P0 | 阶段三 |
| 五 | AI 协作：规划器 + 代码生成/校验/缓存 + 可观测性 | P0 | 阶段四 |
| 六 | P1 功能：AutoDomain/AutoWood/AutoFishing 任务 | P1 | 阶段四 |
| 七 | 导航：小地图定位 + PathExecutor + BetterGI 路径 | P2 | 阶段六 |
| 八 | 战斗：SimpleFighter + 角色识别 + CD | P2 | 阶段六 |
| 九 | 稳定性：长时运行 + 边界 + 拟人化调参 | P3 | 全部 |
| 十 | 运行时决策层（LLM/VLM/人在环）：**接缝已预留**（`g.decide` / `on_uncertain` / `Decider`），见 `docs/design/01-执行引擎.md §11` | P4 | M5 |

**里程碑**：M1 闭环（截图→检测→操作）｜M2 守护+任务体系｜M3 AI 编排可用｜M4 进阶功能｜M5 导航+战斗｜M6 完整版

---

## 11. 风险与注意事项

- **反检测（必须）**：启用拟人化（贝塞尔轨迹+随机延迟+自然点击）；操作间隔 0.8–1.2× 抖动；管理员权限。
- **YOLO 解码**：经源码核实，YoloSharp = 标准 Ultralytics YOLOv8/YOLO11 格式（BGI 无自定义解码）。真正坑：①别再做 sigmoid（已 bake 进图）②布局 `[1,4+nc,N]` 需转置 ③letterbox 反变换。`Predictor.cs` 是 `[Obsolete]` 空壳，真实逻辑在 `BgiYoloPredictor.cs` + YoloSharp NuGet。实现见 `src/abilities/detector.py`（已验证几何解码）。
- **分辨率**：模型与坐标基于 1920×1080，原神须 1080p 窗口模式；启动检查。
- **游戏更新**：UI 变化致模板失效，跟进 BetterGI 社区更新模型/路径。
- **导航精度**：小地图定位精度直接决定导航质量；卡住脱出是稳定性关键。
- **战斗简化**：站桩连招在高难度场景可能不够。
- **AI 编排**：LLM 生成代码需充分校验；规划有 1–3s 延迟，仅用于规划阶段；缓存+沙箱+超时+网络不可用降级。
- **合规**：仅供技术研究学习；使用自动化工具可能违反游戏服务条款。

---

## 12. BetterGI 源码参考索引

| 功能 | BetterGI 路径 | 说明 |
|---|---|---|
| YOLO 推理 | `Core/Recognition/ONNX/BgiYoloPredictor.cs` | 检测封装 |
| YOLO 推理/解码 | `Core/Recognition/ONNX/BgiYoloPredictor.cs` + YoloSharp NuGet | 标准 Ultralytics 格式；`Predictor.cs` 已 `[Obsolete]` 空壳 |
| 模型注册 | `Core/Recognition/ONNX/BgiOnnxModel.cs` | 所有模型路径 |
| OCR | `Core/Recognition/OCR/` | PaddleOCR ONNX |
| 模板匹配 | `Core/Recognition/OpenCv/` | OpenCvSharp |
| 截图 | `Fischless.GameCapture/` | BitBlt/DwmSharedSurface |
| 操作 | `Fischless.WindowsInput/` | PostMessage/SendInput |
| 路径执行 | `GameTask/AutoPathing/PathExecutor.cs` | 导航核心 |
| 相机旋转 | `GameTask/AutoPathing/CameraRotateTask.cs` | 视角旋转 |
| 陷阱脱出 | `GameTask/AutoPathing/TrapEscaper.cs` | 卡住脱出 |
| 小地图定位 | `GameTask/Common/Map/MiniMap/` | 特征匹配 |
| 执行引擎 | `GameTask/TaskTriggerDispatcher.cs` `TaskRunner.cs` | 对照（我们用 Runtime 替代） |
| 战斗 | `GameTask/AutoFight/` | 完整战斗系统 |
| 自动秘境 | `GameTask/AutoDomain/AutoDomainTask.cs` | 秘境自动化 |
| 一条龙 | `GameTask/Common/Job/` | 组合任务（我们用 ctx.run 替代） |

> BetterGI 整体架构分析见 `docs/bgi-framework/00-主体框架.md`。
