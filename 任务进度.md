# 任务进度

> **给新会话的开机文档**。读完本文件 + `CLAUDE.md` 即可接手。
> 维护规则：控制在 ~100 行。详细设计见 `设计实现.md` + `docs/design/`。

## 现状 + 下一步

193 项测试全绿。实机首测完成，定位/场景/OCR/朝向/Q 检测全通。

**下一步：传送地图导航 → g.teleport_to 真实可用 → 自动做日常。**

**当前问题：**
- 全局定位不可靠（无 prev_position 时所有层 score≈0.89-0.91，选错层/偏位置）
- 传送地图导航是占位代码（`_navigate_map_to_target` 只点地图中心，无法拖拽到目标传送点）
- fight/auto_boss/auto_ley_line 未实机验证
- 朝向 0=东 vs target_orientation 0=北 换算待确认

## 阶段路线

| 阶段 | 内容 | 状态 |
|---|---|---|
| **A** | 场景识别+守护+对话 | ✅ 完成 |
| **B** | 导航(定位+传送+移动) | 🔧 定位通,传送占位 |
| **C** | 战斗(血条+连招+Q) | ✅ 骨架完成 |
| **D** | P1 任务(boss+地脉) | ✅ 骨架完成 |
| **E** | AI 口子预留 | ⬜ |

## 当前阻塞项详解

### 1. 传送地图导航（最关键，做日常的前置）
`tp.py._navigate_map_to_target` 是占位代码。需要实现：
- 打开地图后识别当前视口在大地图上的位置
- 拖拽/缩放到目标传送点附近
- 点击传送点图标
- 确认传送（quick_teleport 守护已实现）
对照 BGI `TpTask.cs` 的完整实现。

### 2. 全局定位不可靠
无 prev_position 时模板匹配在 6 层 MapBack 上 score 太接近（0.89-0.91），经常选错层/偏位置。
有 prev_position 时局部搜索 radius=50 color-px 效果好（蒙德城 (3724,378) 连续稳定）。
解决思路：首次定位可先传送到已知锚点，再设 prev_position。

## 代码入口

| 功能 | 入口 |
|---|---|
| 截图+输入 | `context.py` GameContext |
| 场景识别 | `game_state.py` make_classifier |
| 定位 | `position.py` PositionGetter (IMapMatcher+6层MapBack+BGI翻转坐标) |
| 朝向 | `camera.py` CameraControl (IOrientationDetector) |
| 传送 | `tp.py` Teleporter |
| 移动 | `navigator.py` Navigator → `path_executor.py` |
| 战斗 | `fighter.py` SimpleFighter |
| 对话 | `dialog.py` talk/talk_skip |
| 任务插件 | `tasks/` (verify/auto_boss/auto_ley_line) |
| Runtime | `runtime.py` + `daemons/`(8个) |
| 高层 API | `high_level_api.py` g.* |

## 项目结构

```
src/framework/     核心:context·runtime·task·registry·sandbox·authority·observe·policy·scene·config·resources·utils·errors·daemons/(8)
src/abilities/     领域:detector·vision_utils·game_state·dialog·fighter·reward·navigation/(tp·path_executor·camera·position·trap_escaper·navigator)
src/tasks/         插件:ping·compose_ping·auto_boss·auto_ley_line·verify
resources/         资源:templates/·map/(tp.json+Assets/Map 202MB)·paths/(309条)·models/
tests/             193 passed
```

## 实机测试

```bash
python main.py --task verify                              # 只读诊断
python main.py --task verify do_teleport=true waypoint=七天神像-风  # 测传送
```

## 必须记住的真相

- **定位坐标系统**: BGI 翻转坐标系（game_x=Left-px*zoom/Scale，不是 left+px*ptw）；RoughSearchRadius=50 color-px；全局匹配不可靠需 prev_position；`_has_prev` 标志替代 prev_x<=0（坐标可为负数）
- **DPI 缩放**: SourcePlayer 用 enableSizeScale+enableSizeChange(1920,1080) GPU 侧缩放；IScreenCapture 回退需裁剪+缩放；不需要 activateWindow
- **SWIG IImageBuffer**: buildSectorMask/buildCircleMask/maskAnd/maskNot 返回 AvcWrapper.IImageBuffer（无 width/height），需 Image.IImageBuffer(raw) 包装
- **拟人化是框架层**: avc 无 setHumanize，由 utils.py+GameContext 实现
- **YOLO = 标准 Ultralytics**: 别再 sigmoid(已 bake)；[1,4+nc,N] 要转置；letterbox min 比例+(x-pad)/scale 反变换
- **守护 8 个**: frame/scene_estimator/auto_pick/auto_skip/loading_wait/auto_eat/quick_teleport/auto_open_chest
- **OpenCV 下沉 avc**: IMapMatcher/IColorDetector/IOrientationDetector 在 plugins/avc_opencv；MapMatcher NaN 已修（sqrt 负数钳制+isnan 检查）
- **分辨率 1920×1080**；原神管理员运行；仅供技术研究学习
- **BGI 资源**: 模板+tp.json+路径JSON+ONNX 模型已提取；解压用 7z/7zr（py7zr 不支持 BCJ2）

## 变更日志

- **2026-08-06 实机首测+定位修通** — DPI 缩放修复(SourcePlayer GPU 缩放)；定位坐标翻转修复(BGI MapToWorld)；搜索半径从 400→50 color-px；_has_prev 替代 prev_x<=0；MapMatcher NaN 修复；SWIG IImageBuffer 包装修复；verify 增强(守护+OCR+定位双模式)。实机:截图/场景/OCR/朝向/Q/定位全通。193 passed。
- **2026-08-05 阶段 A-D 骨架完成** — 场景识别+守护+对话(A)、导航6文件+309路径(B)、战斗血条+连招(C)、boss+地脉+树脂领取(D)、功能冒烟测试+黄金画面测试+通知系统。196→193 tests。
- **2026-08-04 阶段一~四完成** — GameContext+Detector+vision_utils、Runtime+5守护+可靠性地基、@task+Registry+沙箱、game_state+dialog+守护真实化。
