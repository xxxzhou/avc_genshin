# 任务进度

> **给新会话的开机文档**。读 `CLAUDE.md` + 本文件即可接手。详细设计见 `设计实现.md` + `docs/design/`。
> 维护规则：强调「下一步」与「当前问题」；已完成进度只写大方面。

## 下一步（从这里开始）

**实机验证 `path_executor` 分段执行**：`Navigator.go_to`（连续行走 + 纯相机转向）已实机闭环（2026-08-08，见「近期变更」：60/120 单位分别 11.4s/25.7s 到达、最终 dist 6.0/6.6）；避 pin 链路 + 「边走边小地图连续锚定」也已实机闭环。**剩**：`path_executor` 把一条路径（传送→多 waypoint 分段 go_to）整段跑通的验证（`cache/diag_path.py`）。注意 waypoint 坐标 BGI 文件存 (X=西,Y=北)、加载时已换轴为 (北,西)，读测试 `test_load_waypoint_coordinates`（既有失败项，属路径加载器 x/y 互换，见「当前问题」下方注）。

其余待实机项见「当前问题」#4（fight / auto_boss / auto_ley_line 及各 D 任务流程）。

**生存系统已改进**（2026-08-08）：① AutoEatDaemon 检测间隔 1s→0.15s（对齐 BGI 150ms）+ 吃药间隔 5s→1s + 优先级 25；② 修 INTERACT→GADGET 通道（Z 键不是 F，消除 auto_eat 与 auto_pick 冲突）；③ fighter 新增 `_check_survival()`（rotation 每步前查血量：红血→按 Z 吃药 / 无药→换人保命 / 死亡→按 Z 复活）；④ `recover_on_death()` 传送目标从硬编码"七天神像-风"改为按当前位置找最近 Goddess（`TpDatabase.find_by_type` + 距离排序），位置不可用时回退硬编码；⑤ `SharedState` 新增 `low_hp` 字段（auto_eat 写入、fighter 读取）；⑥ `g.is_low_hp()` 高层 API。⚠ 待实机验证：战斗中红血→吃药/换人是否及时、复活→传送最近神像是否正确。

传送链代码已实现且常量已标定（见下「2026-08-08 实机标定」）：
- `_ZOOM_WHEEL_SIGN=-1` / `_ZOOM_PER_NOTCH=0.083`（实测 scroll(0,+1)×5→delta=-0.417）
- `_DRAG_X_SIGN=1` / `_DRAG_Y_SIGN=1`（实测 +x拖→x增、+y拖→y增）
- `_MAP_SCALE_FACTOR=3.0`（BGI 2.361 是 SendInput 物理像素语义；avc moveTo 逻辑像素 + dpi 补偿后实测拖拽仅 70% 生效，校准到 3.0）
- 坐标轴对换正确（sift=(744,2292) vs 最近锚点(931,2254)，方向一致）
- SIFT 定位 305ms、图标匹配 OK（TeleportWaypoint/Goddess/OneTimeDomain 均命中）

## 当前问题

1. **【已实机闭环】传送链** —— 见「2026-08-08 拖拽修复 + 远距传送闭环」。OCR 面板检测 + 拖拽导航均验证通过；避 pin 路径代码就绪尚未专测，见顶部「下一步」。
2. **【已修，待移动实测】小地图定位 `get_position`** —— 根因是 **MapBack 分层坐标轴颠倒**（`coarse_to_game` 返回 BGI 序 (西,北)，而 prev/SIFT 是 (北,西)=position[0]/position[2]）：① 返回值轴序颠倒（对照 SIFT 看"错 ~7200"）；② `contains`/`game_to_coarse` 收错序 → **局部锚定永远失效** → 兜底全图 → 纳塔地形自相似 → 错。修复：`position.py` `_MapLayer.coarse_to_game`/`game_to_coarse`/`contains` + `_coarse_to_game`/`_game_to_coarse` 回退分支统一 (北,西) 序。实机验证（2026-08-08，角色纳塔七天神像-火）：`verify position` → `sift=(-1368,7760) local(prev=sift)=(-1339,7762) dist=29`。⚠ 已知局限：**冷启动全局匹配**在纳塔有地形自相似歧义（两个高分点 0.954 真 vs 0.959 假，全局可能选错 ~500-800 单位）；正常流程 prev 由传送落点/路径起点 seed（`tp.py`/`path_executor.py`），局部锚定精准。**传送链不受影响**（用大地图 SIFT）。下一步见顶部（移动实测）。
3. **【地脉花传送点选择误差】`screen_to_game` 高 zoom 下误差大** —— 实证：藏金之花实际在摘星崖（锚点 2298,-1509），但 screen_to_game 算出 (2214,-1317) 偏了 ~200 单位，选了"西风之鹰的庙宇"而非摘星崖锚点。根因：花距视口中心 ~400px，zoom=4.37 放大 SIFT 误差。改进：先拖花到视口中心再算坐标，或改用拖拽+图标匹配链路（同 teleport_to）。
3. **朝向/旋转小功能已实机定论并修复（2026-08-08）** —— `get_orientation`(avc IOrientationDetector) **不可用**：原神小地图固定北朝上，它把微差放大成 ±136° 假角度、非相机偏航（实测 0.994 相关度输入却跳 90↔226）。实机定论走**小地图玩家箭头**：`arrow.py` 对称轴法（PCA 主轴 ±50° 扫 101 角取镜像重叠最大轴=对称轴，尖端更窄消 180° 歧义，对称分<0.35 拒非箭头；候选按距中心排序、取第一个对称分达标的——旧"最远点=尖端"启发式在紧凑刻痕箭头上间歇误选底角 ±100° 已弃）。标定：+600px→+26.5° ⇒ **22.6 px/°**，单次封顶 ±600px。`rotate_to` 闭环（读箭头→`move_x=-diff*22.6`→move_by_rel→等停→轻推 W 同步面朝=相机→再读，耗尽后最终读确认）：+90 收敛残差 ~2-5°、-175 开阔地单测收敛 0.5°、复位 ~1°；`max_attempts=int(|diff|/26)+5` 冗余抗单步异常（实测 175° 恰需 9 步，+3 卡边界）。⚠ 局限（游戏机制非 bug）：**原神不能原地转身**，旋转靠轻推 W 走路、每步前进 ~1m，大角度累计位移会撞地形（风起地树/坡）偶发残差 30-100° → 导航层应旋转后重定位/选开阔地。生产代码 `src/abilities/navigation/arrow.py`+`camera.py`。
4. **`fight` / `auto_boss` / `auto_ley_line` 未实机验证**；战斗**检测**已有离线正样本（2026-08-08：血条×16 + enemy identify×15，风起地东 60，见「近期变更」）；`claim_mail` / `craft_resin` / `enter_pot` / `auto_domain` 已过 mock 测试 + 模板误匹配离线筛查 + BGI 逐行对齐核查（阈值 0.8 / ROI 补全 / 坐标改 tp.json），流程仍待实机。
5. **avc 模板匹配偏慢**（朴素标量实现，1080p 全屏 ~13s/模板；ROI 缩小后 ~0.1–1.9s）——实机各任务模板匹配均带 ROI，暂不影响；若实机卡顿需 avc 侧优化（SIMD/匹配方法）。
6. **网上测试图非纯游戏画面** —— fight/leyline/world_map 等叠加演示标注/BGI 日志/工具面板，场景分类（模板匹配）断言受限（实机 OK，见 memory map-template-bug）；识别性断言（YOLO/OCR/血条）对 overlay 不敏感、有效。纯游戏画面留实机自截（`tests/fixtures/screens/` + `test_real_screens.py`）。
7. **avc shader 路径 bug 已修复（2026-08-08）** —— avc 重新构建后 DLL 移到 `plugins/`，`getAvcPath()` 基于实际加载的 avc.dll 路径推导，`plugins/avc.dll` 被 pyd 优先加载导致 assets 拼到 `plugins/assets/`（实际在 root/assets/），shader 加载失败段错误。修复：删掉 `Release/plugins/avc.dll`（pyd 沿 PATH 加载 root/avc.dll）+ `__init__.py` 预加载 root/avc.dll 兜底。⚠ 重新构建 avc 时注意不要再次把 avc.dll 复制进 plugins/。

## 进度（大方面）

| 阶段 | 内容 | 状态 |
|---|---|---|
| A | 场景识别 + 守护 + 对话 | ✅ 完成 |
| B | 导航（定位 + 传送 + 移动） | 🔧 传送链**已实机闭环含远距**（风起地→纳塔 9600 单位 7 轮拖拽收敛 + OCR 面板确认）；**避 pin 已实机闭环**（MARKER→Esc→换候选→TELEPORT）；**小地图定位已修**（轴序颠倒，#2，dist=29）+ **边走边连续锚定已实机闭环**（seed prev 后 max_step=2.8 无跳变，误差~27）；**go_to 已实机闭环**（罗盘帧统一 + 连续行走 + 纯转向，60/120 单位 11.4s/25.7s 到达 dist 6.0/6.6）；剩 path_executor 整段执行实测 |
| C | 战斗（血条 + 连招 + Q） | ✅ 骨架完成 |
| D | P1 任务（boss + 地脉 + 树脂 + 每日奖励） | ✅ 骨架完成（claim_daily_reward + daily 总控 + 6 国冒险家协会路径 + 领邮件/合成树脂/尘歌壶/自动秘境） |
| E | AI 口子预留 | ⬜ 延后（等实机测试全过再开始，见 memory defer-ai-until-real-machine） |

测试：**331 passed / 4 failing / 2 skipped**（失败均为既有无关项：`test_low_hp_red_pixel`+`test_orange_text_detected` MockImageBuffer BGRA/RGBA 不匹配、`test_load_waypoint_coordinates` 路径加载器 x/y 互换、`test_first_active_wins` active slot gap；含传送确认修复新增 15 测 + 7 个既有漂移测试已同步到 2026-08-08 实机标定语义：position 轴对换 / drag scale 3.57 / M 复位 / switch_country v1 禁用）。实机 verify 全跑 **24 OK / 0 ERR**（含传送 + 大地图标定 13 项）。

## 接手入口

- 实机诊断：`python main.py --task verify`（只读）；加 `do_teleport=true waypoint=七天神像-风` 测传送；加 `do_map_calib=true`（先按 M 开图）标定传送地图导航 6 常量。
- 代码：`src/framework`（核心：context·runtime·task·registry·sandbox·daemons/(9)·retry·stuck）；`src/abilities`（领域：detector·game_state·dialog·fighter·reward·daily·mail·craft·pot·domain·navigation/(tp·map_ops·position·camera·navigator·path_executor)）；`src/tasks`（插件：auto_boss·auto_ley_line·claim_daily_reward·claim_mail·craft_resin·enter_pot·auto_domain·daily·verify）。
- 资源：`resources/` 已提取 BGI 模板 + `tp.json` + 309 条路径 + 6 国冒险家协会路径 + ONNX 模型。

## 硬约束（容易踩坑）

- **定位坐标系**：BGI 翻转（`game_x = Left - px*zoom/Scale`，非相加）；坐标可为负，用 `_has_prev` 判初值，**勿用** `prev_x <= 0`。
- **DPI 缩放**：SourcePlayer 走 `enableSizeScale` + `enableSizeChange(1920,1080)` GPU 侧缩放；`IScreenCapture` 回退需裁剪+缩放。
- **SWIG `IImageBuffer`**：mask 系列返回包装类型，无 width/height，需 `Image.IImageBuffer(raw)` 再包一层。
- **拟人化是框架层**：avc 无 `setHumanize`，由 `utils.py` + `GameContext` 实现。
- **YOLO = 标准 Ultralytics**：别再做 sigmoid（已 bake）；`[1,4+nc,N]` 需转置；letterbox min 比例 + `(x-pad)/scale` 反变换。
- **截图必须走 `ctx.capture()`**：SourcePlayer GPU 侧缩放到 1080p；直接 `sc.getBuffer()` 是原始分辨率（如 3206x1876），坐标/模板全错。

## 近期变更

- **2026-08-08** 生存系统改进（回应用户"先把生存问题搞好，那时要加血，那时角色挂了"）：① **AutoEatDaemon 加速**：检测间隔 1s→0.15s（对齐 BGI AutoEatTrigger CheckInterval 150ms）、吃药间隔 5s→1s（对齐 BGI EatInterval 1000ms）、优先级 0→25（对齐 BGI Priority 25）；② **修 INTERACT→GADGET 通道**：Z 键（QuickUseGadget/便携营养袋）不是 F 键（交互），auto_eat 原先声明 INTERACT 与 auto_pick 冲突，改为 GADGET 后两者可并发；③ **fighter 新增 `_check_survival()`**：rotation 每步前调用，红血→按 Z 吃药 / 无药→换人保命（轮询下一槽位，3 秒冷却）/ 死亡→按 Z 复活；全程 try/except 不中断战斗流程；④ **`recover_on_death()` 修最近神像**：传送目标从硬编码"七天神像-风"改为 `TpDatabase.find_by_type("Goddess")` + 当前位置距离排序取最近，位置不可用时回退硬编码；⑤ **`SharedState.low_hp`**：auto_eat 守护每步写入红血状态，fighter 读取（单写多读，GIL 下布尔引用读原子）；⑥ **`g.is_low_hp()`** 高层 API。`authority.py` 新增 `GADGET` 通道枚举。测试 331 passed / 4 既有无关失败（MockImageBuffer BGRA/RGBA 不匹配×2 + waypoint x/y 互换 + active slot gap）。

- **2026-08-08** 战斗检测定论 + 世界敌人识别补进 fighter（回应用户"巡逻遇到怪但没检到"）：**结论——怪当时检到了，只是被脚本 bug 丢弃**。① **血条检测当时就检到**：diag_enemy_find 在 s1（风起地东 60）实机 live 检出 12 条血条（RGBA buffer→avc 正确转 BGR），与 bgi_world 模型 `health bar` 框一一对应（如 ef_s1_04: 模型框 (384,555)-(513,568) cx=448 ↔ 实机血条 (447,562)）。② **bgi_world 世界敌人识别其实在狂检**：颜色校正 + **正确类名**后，265 帧检出 `enemy identify`×15（conf 0.74~0.96，框 w=32~44/h=26~37 远处小怪，血条在其正上方=丘丘人结构）+ `health bar`×16 + chest×18 + unmined ore×19。③ **两个假阴性 bug**：a) **类名键 bug**——模型类名是 **`"enemy identify"`（带空格）**，diag 脚本全查 `"enemy_identify"`（下划线）→ 永远空 → 显示"敌人×0"（src 不受影响：fighter 用血条检测）；b) **PNG 存图 R/B 互换 bug**——diag 脚本 `arr[:,:,:3]` 把 RGBA buffer 当 BGRA 存 → 离线喂模型颜色错乱 → 之前 bgi_world 0 检出是假阴性。④ **代码**：`fighter.py` 新增懒加载 `_world_detector()`（bgi_world.onnx conf=0.3）+ `find_enemies()`/`has_enemy_in_world()`（`Detection` 列表，含发呆怪；`has_enemy` 保持血条语义不混，战斗清场/场景估计仍正确）；`high_level_api.py` 加 `g.scan_enemies()`/`g.has_enemy_in_world()` 桥 + `_run` 支持 kwargs；`cache/diag_enemy_find.py` 键名改 `"enemy identify"`。⚠ 架构注：`has_enemy`（战斗态=血条）与 `find_enemies`（世界态=模型）分层——发呆怪无血条，只有模型检得出；巡逻扫描用后者。测试 334 passed（1 既有无关失败）。诊断 `cache/diag_world_detect3.py`（颜色校正+正确键名全帧扫描）、`analyze_bar*.py`（精确血条结构分析）、`diag_channel.py`。**实机 live 闭环（2026-08-08 同日晚）**：重跑 `diag_enemy_find`（键名已修）——传送风起地 OK → 巡逻东 5 段 → **s5(东300) 敌人×3（conf 0.90-0.91，框 w=36~44/h=26~33）+ 血条×23** → 盲走逼近+普攻 → **战斗扫描敌人×3 + 血条×20（✅✅ 正样本）**。确认 live 下 bgi_world YoloDetector 与 detect_blood_bars 都工作正常（无需 RGBA 手动转色）。⚠ 顺带记录 **detect_blood_bars 假阳性**：s2 检出 13 条"血条"全是 cy≈372 同一行、w 3-23/h 4-5 的远处红色景物（BGI FindBloodBars 同样不过滤形状，属已知干扰；战斗态判定时景物干扰低，暂不加过滤以免漏真实小血条）。另注：s1~s4 敌人×0 而 s5 敌人×3，是**野外怪游走**（两次巡逻怪位置不同），非检测失效。实机传送首跑曾因游戏停在不识别界面 scene=UNKNOWN 跳过导航失败（瞬时状态，非代码 bug；重跑即好）。
- **2026-08-08** 移动 go_to 实机验证修复（小功能）：**① 根因** —— `target_orientation` 用 BGI 帧 atan2(Δ西,Δ北)（西=90°，逆时针罗盘），而箭头传感器是真罗盘（东=90°），两者东西镜像 ⇒ go_to 拿 target_orientation 与箭头相减转反方向（diag_moveto B：目标在西却越走越远 dist 80→108）。**修复**：`target_orientation` 改真罗盘帧 `atan2(-Δ西, Δ北)`（0=北/90=东，与 get_orientation 箭头同帧；单测同步更新，BGI 帧仅作 acos 数学参考）。**② 根因** —— go_to 每轮 `rotate_to_approach` → `_nudge_sync` 内部 `keyUp(W)` 打断外层持续按住 W ⇒ 爬行。**修复**：实机证实**移动中面朝自动同步相机**（diag_moveto A：按住 W 时 +600px → 面朝 +21.3°），go_to 改「持续按住 W + 每轮 `rotate_camera_to_target`（纯 move_by_rel 转向、不 nudge、不打断行走）」，删初始 rotate_to（空闲大角度轻推走路撞地形）+ 删 `rotate_to_approach`（唯一调用方已换）。**③ 到达阈值**：`_ARRIVE_DISTANCE` 4→8（移动中定位相邻采样噪声 ~±4）。实机：diag_go_to 正北 60→11.4s 到达 dist=6.0、正北 120→25.7s 到达 dist=6.6（≈4.7-5.3 单位/s 正常行走）。测试 334 passed。诊断 `cache/diag_moveto.py` `diag_go_to.py`。
- **2026-08-08** 朝向/旋转小功能实机定论并提升生产（当前问题#3 关闭）：**根因**：avc `get_orientation` 读小地图像素差放大值、固定北朝上 ⇒ 假角度不可用；自研箭头检测旧"最远点=尖端"启发式在紧凑刻痕箭头上间歇误选底角（±100° 跳变）。**修复**：`arrow.py` 对称轴法（PCA 主轴 ±50° 扫镜像重叠最大轴=对称轴、尖端更窄消歧义、对称分<0.35 拒非箭头、候选取第一个达标者）；`camera.py` 重写为箭头闭环 `rotate_to`（`move_x=-diff*22.6px` 封顶 ±600 + 轻推 W 同步面朝=相机 + 耗尽后最终读确认 + `max_attempts=+5` 冗余）。实机：+90 收敛 2-5°、-175 开阔地 0.5°、复位 ~1°；大角度偶发撞地形（原神不能原地转身，旋转靠走路）残差 30-100° 已文档化为导航层责任。诊断 `cache/diag_rot175.py` `diag_rotate_prod.py` `diag_rotfix2.py` `diag_rotloop.py`。测试 334 passed（1 既有无关失败）。
- **2026-08-08** 世界内移动「边走边小地图连续锚定」实机闭环（对应上条"下一步"第二项）：角色在清泉镇锚点（SIFT 真值 (2020,-880)）。**关键对照**：不 seed prev 时新建 `PositionGetter` 走全图匹配 → 风起地草地自相似 → 返回稳定但绝对错的 (-1477,10089)（west=10089 超提瓦特范围）；**seed prev 后**（生产流程=传送→`_set_prev_position`→局部匹配）静止 dist_to_truth≈27（与已知 ~29 一致）、漂移 1.2；按住 W 走 3 秒连续采样 (2046,-877)→(2037,-878) 坐标单调连续、相邻步长 0.1~2.8（max_step=2.8、total=9.5）**无跳变**，停走后回到真值附近 dist=14.9 → 「边走边锚定」验证通过。⚠ 未验项：`Navigator.go_to` 整循环（旋转→按 W→到达判据 `_ARRIVE_DISTANCE=4` < 定位误差 ~27，到达判据需实测斟酌）+ `path_executor` 分段执行。诊断 `cache/diag_move.py`（SIFT 取真值 seed prev → 静态基线 → 3 秒 W 采样 → 步长/方向分析）。
- **2026-08-08** 避 pin 链路实机闭环（对应上条"下一步"避 pin 专测）：玩家视口内实机扫到 **4 个 pin 覆盖图标 + 11 个干净图标**，驱动生产 `_click_and_confirm_teleport` 完整走通避让链——点击候选0 @(929,380) → **`panel=MARKER`** → `close_marker_panel`（只按 Esc）返回 False → **换下一候选** @(1131,482) → **`panel=TELEPORT`** → 点击传送按钮 @(1689,1007) → 真实传送到 传送锚点(清泉镇/苍风高地)（落点 SIFT (2028,-868) ≈ DB (1993,-874)）。`detect_tp_panel` MARKER/TELEPORT 分类、`close_marker_panel`、候选重试循环三段全部实机验证。诊断 `cache/diag_avoid_pin.py`（安全版：只扫玩家视口不导航不拖图，点击前 `to_screen` 越界/左上角 UI 覆盖区守卫；v1 因导航 `drag_map` 的 `moveTo` 绝对坐标在 SIFT 失败时把鼠标甩到窗口外已删除回退）。⚠ 顺带记录：真机脚本被打断易留 Esc 菜单（帕蒙菜单）挡输入，`measure_zoom_level` 为 None 时先按 Esc 恢复大世界再按 M。
- **2026-08-08** 修小地图定位（当前问题#2 关闭，对应上条"下一步"）：根因 = **MapBack 分层坐标轴颠倒**。`position.py` `_MapLayer` 的 `coarse_to_game` 返回 BGI 序 (西,北)（`Left` 在西轴/`Top` 在北轴），而框架统一约定是 (北,西) = `position[0]/position[2]`（`_map256_to_game`/`get_position_from_big_map` 同此）。后果双杀：① `get_position` 返回值轴序颠倒（对照 SIFT 看去"错 ~7200"）；② `contains`/`game_to_coarse` 把 (北,西) 当 (西,北) 收 → **prev 永远不在层覆盖内 → 局部锚定永不生效** → 兜底全图 → 纳塔地形自相似 → 匹配错点。修复：`_MapLayer.contains`/`coarse_to_game`/`game_to_coarse` 三方法 + `_coarse_to_game`/`_game_to_coarse` 回退分支全部统一 (北,西) 序（`Left`→西轴、`Top`→北轴，`game_x=Left-px*zoom/Scale` 语义保留，仅换返回/入参轴序）。用 7 国中心验证层覆盖全部落对（纳塔→L0、蒙德→L2、稻妻→L3、须弥/枫丹→L1、挪德卡莱→L5）。诊断 `cache/diag_minimap.py`（逐步打印朝向/掩码/每层 local+global 分 vs 真值，首跑 L0 contains_truth=False、local==global 坐实轴序 bug；修复后 contains_truth=True、L0 local dist=29）。`verify` 的 `position` 探针改为：先全局 → 开图 SIFT 实测真值 → 关图 → 以真值 seed prev 做局部匹配输出 dist（实机 `sift=(-1368,7760) local=(-1339,7762) dist=29`），不再用固定 waypoint 锚点（角色不在该锚点时局部结果无意义）。⚠ 已知局限：冷启动全局匹配在纳塔有地形自相似歧义（真 0.954 vs 假 0.959），正常流程 prev 由传送落点 seed、局部锚定为主路径。测试 335 passed。诊断脚本 `cache/diag_minimap.py` `diag_pos_seed.py` `diag_pos_e2e.py` `diag_mask.py` `diag_ambiguity.py` `diag_wrong_prev*.py`。
- **2026-08-08** 拖拽修复 + 远距传送闭环 + 小地图定位确认坏（全跑验证 24 OK/0 ERR 暴露）：**① 拖拽 bug 根因** —— `_MAP_DRAG_START=(960,540)` 在视口中心，而地图打开时**玩家位置标记就在中心，Genshin 拦截从玩家标记上发起的拖拽**（calib_drag Δ=0 / 手动拖中心 0.3% vs 非中心 49-64% 像素差；monkeypatch 换 (960,650) 四方向 SIFT 跟踪 ±200-240）。修复：`map_ops.py` `_MAP_DRAG_START` 改 `(960,650)`。**② 远距传送闭环**：风起地→纳塔七天神像-火（~9600 单位）导航 7 轮收敛（dist 9619→7455→5403→3534→1423→451→90），SIFT 一路跟踪，命中 Goddess 图标→panel=TELEPORT→点传送按钮→落点 (-1400.8,7759.6)。**③ 小地图定位确认坏**（当前问题#2）：角色在纳塔（大地图 SIFT (-1368,7760) 正确）但 `get_position` 返回 (5855,1201)/(7889,-633) 错 ~7200，连局部匹配也失败兜底全图。**传送链用大地图 SIFT 不受影响；世界内移动依赖小地图定位，待修**。证据 `debug/minimap_natlan_156.png`。诊断脚本 `cache/diag_drag_{probe,pixel,start,dir,compare,focus}.py`。测试 335 passed。
- **2026-08-08** 实机传送闭环 + 鼠标坐标修复（决定性根因，对应上条「下一步」）：**① 鼠标偏左上 bug 根因** —— 本进程 DPI-unaware（系统 DPI=96）→ `GetWindowRect/GetClientRect` 返回物理像素；而 avc `sc.toScreen` 用窗口 DPI（240）把坐标逻辑化（span 缩小 1/dpi），与 `ic.moveTo`/`getCursorPos`（物理像素）不同空间，导致传送到屏幕左上。修复：`context.py` 重写 `to_screen`（新增 `_window_geometry()` 每次现算 Win32 物理几何 `win_l/win_t/side_border/top_border/cli_w/cli_h` + 逻辑/物理 DPI 启发式判定，1080p 线性映射到客户区，绕开 `sc.toScreen`）+ `_calc_window_offsets` 不再二次乘 DPI（先 `_dpi_scale=dpi/96` 再存边框）。用 `cache/diag_cursor_probe.py` 探针验证：8 个测试点鼠标落点 vs Win32 客户区理论位置 **全部 ±1px**。**② 实机传送闭环**：`verify do_teleport=true waypoint=七天神像-风` 全链路成功——导航收敛（iter0 dist=544→iter1 dist=152）、4 个 Goddess 候选、点第一个 `@(1014,454)`→**`panel=TELEPORT`**→点传送按钮 `@(1689,1007)`→传送到 `(1854.05,-578.44)`（目标 1859,-576），15/15 verify 探针全 OK。当前问题#1 关闭。下一步：避 pin 路径专测（见顶部）。
- **2026-08-08** 修复传送确认（OCR 面板检测 + 避 pin，对应上条探查的下一步）：新增 `abilities/tp_panel.py`（`TeleportPanelKind` NONE/TELEPORT/MARKER + `detect_tp_panel` OCR 分类 + `find_teleport_button` + `close_marker_panel` 只按 Esc；⚠ 标记面板 '确认' 按钮带 F 快捷键，**绝不按 F**）；`map_ops.py` `find_tp_icon`→`find_tp_icons` 返回按距视口中心排序的候选列表；`tp.py` `_navigate_map_to_target` 返回候选、新增 `_click_and_confirm_teleport`（遍历候选→点击→OCR 检测→标记面板 Esc 关闭换下一候选 / 传送面板点按钮或按 F，超时换点）+ `_wait_and_confirm_teleport` 改 OCR；`daemons/quick_teleport.py` 同步改用 OCR 面板检测。测试：新增 `TestTpPanelDetect`(7) + `TestTeleportConfirmFlow`(8) 全通过；另把 7 个既有漂移测试同步到标定语义（position 轴对换、drag scale 3.0→3.57、tp.py M 复位循环、switch_country v1 禁用），全套 335 passed / 0 failing。
- **2026-08-08** 传送链实机探查（决定性根因）：点开的地图卡片是**自定义标记面板**（OCR '追踪'/'删除'/'标记（点击更改标记名称）'，玩家 114 个标记覆盖传送点），非传送面板。实验脚本在 `Temp/claude/`：`test_panel_seq.py`（点击图标后 OCR 全帧，证明标记面板）、`test_fkey.py`（点 Goddess@(1176,744)→面板有 F 提示→按 F→地图未关未传送）、`test_waypoint.py`（dump 全部传送图标匹配，点 TeleportWaypoint@(372,638) 无面板）、`diag_confirm.py`（GoTeleport.png 实机仅 0.588）。同时确认：`ctx.click_at` 内部走 to_screen 正确转换（`ctx.ic.click` 直接吃屏幕坐标勿用于 buffer 坐标）；`ctx.save_debug` 存原始窗口分辨率（3206×1876），调试图坐标需缩放约 1.67×/1.74× 才对应 1080p。下一步：修确认逻辑（OCR 面板检测 + 避 pin）见顶部「下一步」。
- **2026-08-07** BGI 对齐核查（B1-B6，`docs/design/08-BGI对齐核查.md`）：①秘境坐标改查 tp.json（BGI DomainPositionMap 同源，35 个可刷秘境，旧 4 硬编码坐标 3 错 1 无效，绝缘之境=椛染之庭别名）；②mail/craft/pot/domain 模板阈值统一 0.8（BGI 默认，规避 E1 见 0.71 误匹配）；③pot 4 模板补 BGI ROI（potLove/potMoney/potPageClose/pageCloseWhite，兼治 avc 全屏慢匹配）+ 退出先点 PageCloseWhite；④domain 确认按钮 btn_white_confirm→confirm.png（BGI AutoFight，右半 ROI）+ party_btn_choose_view ROI；⑤mail 开头加回主界面；⑥路径资源核对齐全（craft 4 国/boss/guild 6 国/ley_line 319 条）。测试 320 passed。
- **2026-08-07** 五方向推进（D 可靠性 + C 守护 + A 日常补齐 + B 自动秘境 + E 离线验证）：`framework/retry.py`（`retry_on` 指数退避装饰器 + `retry_until` 轮询，NormalEnd/CancelledError/PolicyViolation 不重试）+ `framework/stuck.py`（`StuckDetector` 连续 N 次同值判卡死）+ `daemons/auto_talk.py`（NPC F 交互，priority=5 高于 auto_pick）+ `abilities/mail.py`（领邮件 `claim_all_mail`）+ `abilities/craft.py`（合成浓缩树脂 `craft_condensed_resin`）+ `abilities/pot.py`（尘歌壶进入/领奖/退出）+ `abilities/domain.py`（自动秘境 enter/fight/claim/exit，4 秘境坐标）+ 4 任务插件（claim_mail/craft_resin/enter_pot/auto_domain）+ `daily.py` 总控补全（领邮件→合成树脂→自动秘境→首领→地脉→每日奖励→尘歌壶，步骤失败不阻断）+ `game_state.py` 新增 18 模板条目 + 18 个 `has_*` 原语；资源复制 12 模板（esc_mail_reward/craft_condensed_resin/sereniteapot_love 等）+ 4 合成台路径；修 `_resolve_template_path` 兼容 `res.template_ui()` 相对路径（避免二次拼接）；E1 `cache/verify_template_match.py` 960×540 降采样 + ROI 分组验证 20 新模板 6 张真实图**无误匹配 ≥0.8**（最高 0.71，avc 朴素模板匹配 1080p 全屏 ~13s/模板偏慢，实机需 ROI）。`tests/test_retry.py`(23) + `test_phase_daily_new.py`(22) 新增。测试 **320 passed / 0 failing / 2 skipped**。
- **2026-08-07** 每日奖励领取领域能力 + 任务插件 + 总控编排：`abilities/daily.py`（F1 冒险之证直领 `claim_encounter_points` + 凯瑟琳对话回退 `claim_daily_at_guild` + 派遣一键领取/重探 `one_key_expedition` + 总控 `claim_daily_reward`）+ `tasks/claim_daily_reward.py`（任务插件）+ `tasks/daily.py`（每日总控：auto_boss→auto_ley_line→claim_daily_reward，其余留 TODO）+ `resources/paths/guild/` 6 国冒险家协会路径（蒙德/璃月/稻妻/须弥/枫丹/挪德卡莱，从 BGI 复制）+ `tests/test_daily.py`（29 测：领域能力各函数 + 任务插件 + 总控编排 + Registry 可发现）。测试 269 passed。（B 首批）：avc 检测能力端到端离线验通——`cache/verify_yolo_detector.py`（bgi_world 7 类：health bar/enemy identify/drops/ore/unmined ore/chest/locked chest，**不含怪物类**，怪物走头顶血条检测；fight 图检出 health bar+ore，框 78×7 横条证 `[1,4+nc,N]` 转置对、center.y 落上半部证 letterbox 反变换对、分数 0.84/0.64/0.43 不饱和证未重做 sigmoid——**3 坑均未踩**）+ `cache/verify_ocr.py`（PP-OCRv6 认出"原粹树脂"/"地脉之花"等 score≥0.985，OCR path 修复后链路正常）。`test_web_screens` manifest 加 fight `yolo_min:{health bar:1}` + leyline `contains_text:[原粹树脂]`（detector+OCR 从"诊断打印"转真断言）。测试 240 passed。
- **2026-08-07** 朝向换算核对闭环：读 BGI 源码确认 `get_orientation` 与 `target_orientation` 同坐标系（BGI `RotateToApproach` 直接相减 + FromGia/PredictRotation/GetTargetOrientation 三同系），`camera.py` docstring 修正"待实机标定"→"同系无需换算"；新增 `tests/test_camera_orientation.py`（13 测：target_orientation≡BGI acos 公式 + `_angle_diff` + `_control_ratio`）。当前问题#3 关闭。
- **2026-08-07** avc 大地图 SIFT 定位对齐 BGI（底图特征预存 + 切块局部搜索）：avc `IFeatureMatcher` 加 3 方法（`loadTrainFeaturesPath`/`matchQueryLocal`/`matchQueryFull`，复刻 BGI `.kp.bin`/`.mat.png` 预存 + 60×88 切块 + FLANN KnnMatchLocal/Match），`position.py` 改用新接口（删 addReference/match 全图重算、加 `_build_search_rect` + `_load_train_features`），`tp.py` 传 `expected_center`（首轮全图、后续切块局部）。测试 226 passed。
- **2026-08-07** 传送地图导航实现：`position.py` SIFT 视口重定位（`get_position_from_big_map`）+ 新建 `map_ops.py`（`MapController`：zoom 测量/滚轮缩放/拖拽/地面层切换/7 国切换/图标匹配）+ `tp.py` 重写 `_navigate_map_to_target`（MoveMapToCore 循环）+ `_wait_and_confirm_teleport` 模板匹配替代硬编码 + `path_executor.py` 传送后锚定 navigator prev + 修 `test_readonly_probes`（MockPos 补 `set_prev_position`）；`verify` 加 `do_map_calib` 6 项标定探测。测试 217 passed。
- **2026-08-06** 实机首测 + 定位修通（DPI 缩放 / 坐标翻转 / MapMatcher NaN / SWIG 包装修复）。
- **2026-08-08** 实机标定传送地图导航 6 项常量（`verify do_map_calib=true`，21 项全 OK）：滚轮方向/每槽步长（`_ZOOM_WHEEL_SIGN=-1`/`_ZOOM_PER_NOTCH=0.083`，实测 scroll(0,+1)×5→delta=-0.417；⚠ 大槽数单次 scroll 被游戏吞掉，必须逐槽）、拖拽方向（X/Y sign=1）、`_MAP_SCALE_FACTOR` 2.361→3.0（BGI 值 SendInput 物理像素语义，avc moveTo 逻辑像素需 dpi 补偿）、坐标轴对换正确。新增 F9 全局取消热键（`framework/hotkey.py`：RegisterHotKey + GetAsyncKeyState 双保险，非管理员/占用时轮询兜底；Runtime `_start_hotkey`/`_stop_hotkey` 集成）+ 单行状态显示（`framework/status.py`：verify 改为覆盖式单行 + 5 秒自动清除）。修 avc shader 路径 bug（见当前问题#7）：删 `plugins/avc.dll` + `__init__.py` 预加载 root/avc.dll。测试 308 passed（phase_b 92 个含新增标定断言）。下一步：实测传送闭环。
- **2026-08-05** 阶段 A–D 骨架完成。
- **2026-08-04** 框架地基（context / runtime / @task / sandbox / game_state）。
