# 任务进度

> **给新会话的开机文档**。读完本文件 + `CLAUDE.md` 即可接手。
> 维护规则：每完成一个阶段/任务,①更新下方状态表 ②在「变更日志」追加一条 ③控制在 ~100 行。
> 详细设计见 `设计实现.md` + `docs/design/`,真实进度以本文件为准。

## 一句话现状

阶段 B 骨架完成：导航模块6个文件 + 309条路径JSON + g.teleport_to/go_to 已接线。小地图常量已修正为 BGI 实际值(62,19,212,212)。OpenCV 图像处理（摄像机朝向/小地图定位）决定下沉到 avc C++ SDK 层实现，avc_genshin 只做调用。下一站是**阶段 C(战斗)**。

## 方向调整说明

原计划阶段五是 AI 协作层(规划器/代码生成/缓存),用户决定暂缓——AI 客户端设计未定,先把 BetterGI 同等功能做出来。AI 层只留 `g.decide`/`Planner`/`TaskGenerator` 接口口子,不实现。

## 新阶段路线(A→B→C→D→E,按依赖顺序)

| 阶段 | 内容 | 解锁什么 | 状态 |
|---|---|---|---|
| **A** | 场景识别 + 资源对接 + 守护真实化 + 对话能力 | 守护能真正激活;`g.talk/talk_skip` 可用 | ✅ 完成 |
| **B** | 导航(PathExecutor + 小地图定位 + 传送) | `g.teleport_to/go_to` 可用;任务能"走到地方" | ✅ 骨架完成 |
| **C** | 战斗(SimpleFighter + 角色识别 + CD检测) | `g.has_enemy/find_nearest_enemy` 真实化;打怪任务能跑 | ⬜ **当前** |
| **D** | P1 任务(auto_domain/auto_wood/auto_fishing/auto_boss/auto_ley_line) | 真实游戏自动化 | ⬜ |
| **E** | AI 口子预留(Planner/TaskGenerator Protocol + 失败回流 hook) | AI 大模型可接入的接缝 | ⬜ |

依赖链：A 是一切基础(没场景识别守护不激活)→ B 解锁移动 → C 解锁战斗 → D 才能写真实任务。E 贯穿始终。

## 阶段 B 关键信息(导航)

- **已完成**: tp.py(TpDatabase+Teleporter)、path_executor.py(Waypoint+PathTask+PathExecutor)、camera.py(CameraControl)、position.py(PositionGetter)、trap_escaper.py(TrapEscaper)、navigator.py(Navigator)
- **g.teleport_to/go_to 已接线**: 不再 NotImplementedError
- **小地图定位待完善**: SIFT 数据(50-500MB)需从 BGI release 包下载;SIFT 匹配链计划在 avc C++ SDK 层实现（OpenCV 处理下沉到 avc）
- **摄像机朝向待完善**: CameraOrientationFromGia 算法（极坐标+Scharr+峰值卷积）已移植到 camera.py 的 `compute_orientation()` 函数，但决定将 OpenCV 处理下沉到 avc C++ SDK 层，avc_genshin 只做调用
- **小地图常量已修正**: 从错误的 (24,12,280,280) 修正为 BGI 实际值 (62,19,212,212)（对照 MapAssets.MimiMapRect1080P）
- **BGI 路径 JSON 已复制**: 48 个 boss + 261 个地脉异常路径

## 阶段 C 关键信息(战斗)

- `SimpleFighter.fight(duration_s)` — 站桩连招:普攻→Q→E→切人,固定 rotation
- 角色识别:`avatar_side.onnx`(分类模型,argmax)
- CD/能量检测:`q_classify.onnx`(Q 技能是否可用) + 技能图标模板匹配
- BGI 有完整战斗脚本引擎(JSON rotation + 元素反应),我们起步简化

## 已完成:交付要点(找代码的入口)

**一·基础链路** — `context.py`(GameContext:懒导 avc/capture/坐标转换)、`detector.py`(GenshinDetector,onnxruntime)、`vision_utils.py`(模板/OCR 包装)、`_proto.py`(原型探针)。
**二·操作闭环** — 拟人化在 `utils.py`+`GameContext.click_at`,avc 绑定无拟人化故框架自实现。
**三·Runtime+地基** — `runtime.py`(asyncio loop+同步桥+守护框架驱动循环)、`daemons/`(8个)、`authority.py`/`observe.py`/`policy.py`/`scene.py`/`shared.py`/`cancellation.py`。
**四·任务体系** — `task.py`(@task)、`sandbox.py`(AST 白名单)、`registry.py`(discover/热加载)、Runtime.`run_task/_run_inline`;示例 `tasks/ping.py`、`compose_ping.py`;CLI `--task`。
**A·场景+守护+对话** — `game_state.py`(make_classifier+12个特征原语)、`dialog.py`(talk/talk_skip/visible_options)、守护真实化(auto_skip+auto_pick升级)+3新守护(auto_eat/quick_teleport/auto_open_chest)、95个模板+tp.json、`g.talk/talk_skip`已接线。
**B·导航** — `navigation/tp.py`(TpDatabase+Teleporter,地图UI传送)、`path_executor.py`(Waypoint+PathTask+路径分割+加载)、`camera.py`(CameraControl,朝向+旋转)、`position.py`(PositionGetter,坐标转换+SIFT骨架)、`trap_escaper.py`(TrapEscaper,卡死检测+脱困)、`navigator.py`(Navigator,移动循环)、`g.teleport_to/go_to`已接线。

## 项目地图

```
src/framework/     核心:context·runtime·task·registry·sandbox·authority·observe·policy·scene·config·resources·utils·errors·daemons/(8)
src/abilities/     领域:detector(YOLO)·vision_utils(模板/OCR)·game_state(场景)·dialog(对话)·navigation/(tp·path_executor·camera·position·trap_escaper·navigator)
src/tasks/         持久任务插件:ping·compose_ping  ← auto_domain/wood/fishing/boss/ley_line 待建
resources/         游戏资源:templates/(7子目录)·map/tp.json·paths/(boss·ley_line,309条) ← SIFT 数据+ONNX 模型待获取
tests/             112 项:reliability(17)·runtime(5)·tasks(13)·phase_a(30)·phase_b(47)
main.py            入口:--proto / --task / --intent(未实现)
```

## 必须记住的真相

- **拟人化是框架层**:avc Python 绑定无 `setHumanize`,已由 `utils.py`+`GameContext` 实现。
- **YOLO = 标准 Ultralytics**:3 坑:①别再 sigmoid(已 bake)②`[1,4+nc,N]` 要转置 ③letterbox min 比例+`(x-pad)/scale` 反变换。类名从 ONNX 元数据 `names` 读。
- **场景分类器是闭包**:``make_classifier(ctx)`` 捕获 ctx 用于模板匹配;Runtime 启动时注册;无 avc 时退回默认(UNKNOWN)。
- **守护 8 个**:frame/scene_estimator/auto_pick/auto_skip/loading_wait/auto_eat/quick_teleport/auto_open_chest。
- **导航 v1 简化**:传送用地图 UI 操作(非 BGI 的缩放/拖拽/图标匹配);移动循环骨架就位但定位/朝向需 SIFT 数据后完善。
- **OpenCV 处理下沉 avc**:摄像机朝向/小地图定位等 OpenCV 图像处理决定在 avc C++ SDK 层实现，avc_genshin 只做调用。camera.py 已有 `compute_orientation()` 的 Python 参考实现，待 avc 提供对应 C++ API 后替换。
- **依赖钉版本**:`onnxruntime>=1.20.1,<1.23`、`numpy<2`。
- **分辨率 1920×1080**;原神管理员运行;仅供技术研究学习。
- **BGI 资源**:模板图+tp.json+路径JSON已复制;ONNX 模型+全地图 SIFT 数据需从 release 包获取。

## 变更日志(最新在上)

- **2026-08-04 阶段 B 补充** — 小地图常量修正为 BGI 实际值(62,19,212,212);CameraOrientationFromGia 算法移植到 camera.py 作参考实现;决定 OpenCV 图像处理下沉到 avc C++ SDK 层，avc_genshin 只做调用。
- **2026-08-04 阶段 B 骨架完成** — navigation/(tp·path_executor·camera·position·trap_escaper·navigator)6个文件;TpDatabase+Teleporter+Waypoint+PathTask+PathExecutor+CameraControl+PositionGetter+TrapEscaper+Navigator;g.teleport_to/go_to接线;309条路径JSON(boss 48+地脉 261);47项新测试,总数112全过。小地图定位/摄像机朝向的图像处理链待 SIFT 数据就绪。
- **2026-08-04 阶段 A 完成** — game_state.py(12特征+make_classifier)+dialog.py(talk/talk_skip/visible_options)+3新守护(auto_eat/quick_teleport/auto_open_chest)+auto_skip/auto_pick升级+95模板+tp.json+g.talk/talk_skip接线;30项新测试,总数65全过。
- **2026-08-04 方向调整** — 暂缓 AI 协作层(阶段五),先对标 BetterGI 功能。新路线 A→B→C→D→E:场景识别→导航→战斗→P1任务→AI口子。BGI 全地图数据需从 release 下载(另一台机器进行中)。
- **2026-08-04 阶段四完成** — @task+Registry+沙箱+ctx.run;修 3 个 bug;13 项任务测试,总数 35 全过。
- **2026-08-04 阶段三完成** — Runtime+5 守护+可靠性地基;22 项测试;订正文档。
- **2026-08-04 阶段一·二完成** — GameContext+Detector+vision_utils;核实 avc 真实绑定。
