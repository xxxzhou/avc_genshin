# 任务进度

> **给新会话的开机文档**。读 `CLAUDE.md` + 本文件即可接手。详细设计见 `设计实现.md` + `docs/design/`。
> 维护规则：强调「下一步」与「当前问题」；已完成进度只写大方面。

## 下一步（从这里开始）

**验证避 pin 路径（标记覆盖传送点）**：主路径已实机闭环（见下「2026-08-08 实机传送闭环」），但玩家 114 个自定义标记随时可能命中 pin，需要实测一次「点中标记面板 → Esc 关闭 → 换下一候选」的完整避让链。可在开着自定义标记覆盖的传送点附近跑：`python main.py --task verify do_teleport=true waypoint=七天神像-风`，观察日志是否出现 `panel=MARKER`→`close_marker_panel`→重试下一候选。

其余待实机项见「当前问题」#2/#4（全局定位可靠性 / fight·auto_boss·auto_ley_line 及各 D 任务流程）。

传送链代码已实现且常量已标定（见下「2026-08-08 实机标定」）：
- `_ZOOM_WHEEL_SIGN=-1` / `_ZOOM_PER_NOTCH=0.083`（实测 scroll(0,+1)×5→delta=-0.417）
- `_DRAG_X_SIGN=1` / `_DRAG_Y_SIGN=1`（实测 +x拖→x增、+y拖→y增）
- `_MAP_SCALE_FACTOR=3.0`（BGI 2.361 是 SendInput 物理像素语义；avc moveTo 逻辑像素 + dpi 补偿后实测拖拽仅 70% 生效，校准到 3.0）
- 坐标轴对换正确（sift=(744,2292) vs 最近锚点(931,2254)，方向一致）
- SIFT 定位 305ms、图标匹配 OK（TeleportWaypoint/Goddess/OneTimeDomain 均命中）

## 当前问题

1. **【已实机闭环】传送链确认步骤** —— 见「2026-08-08 实机传送闭环」。OCR 面板检测主路径验证通过（panel=TELEPORT→点按钮→传送成功）；避 pin 路径（panel=MARKER→Esc→换候选）代码就绪但尚未专测，见顶部「下一步」。
2. **全局定位可靠性** —— 传送后已 `set_prev_position(tran_x, tran_y)`（tp.py + path_executor.py 双锚定），缓解无 prev 时 6 层选错层；待实机确认。
3. **朝向换算已核对（2026-08-07）** —— `get_orientation`(avc/FromGia) 与 `target_orientation`(atan2≡BGI GetTargetOrientation) **同坐标系**，`_angle_diff` 直接相减即可、无需换算（依据：BGI `RotateToApproach` 直接 `(cao-target)` 相减 + FromGia/PredictRotation/GetTargetOrientation 三同系；13 单测锁定 ≡ BGI acos 公式）。avc 输出数值层面待实机/离线验证套件。
4. **`fight` / `auto_boss` / `auto_ley_line` 未实机验证**；`claim_mail` / `craft_resin` / `enter_pot` / `auto_domain` 已过 mock 测试 + 模板误匹配离线筛查 + BGI 逐行对齐核查（阈值 0.8 / ROI 补全 / 坐标改 tp.json），流程仍待实机。
5. **avc 模板匹配偏慢**（朴素标量实现，1080p 全屏 ~13s/模板；ROI 缩小后 ~0.1–1.9s）——实机各任务模板匹配均带 ROI，暂不影响；若实机卡顿需 avc 侧优化（SIMD/匹配方法）。
6. **网上测试图非纯游戏画面** —— fight/leyline/world_map 等叠加演示标注/BGI 日志/工具面板，场景分类（模板匹配）断言受限（实机 OK，见 memory map-template-bug）；识别性断言（YOLO/OCR/血条）对 overlay 不敏感、有效。纯游戏画面留实机自截（`tests/fixtures/screens/` + `test_real_screens.py`）。
7. **avc shader 路径 bug 已修复（2026-08-08）** —— avc 重新构建后 DLL 移到 `plugins/`，`getAvcPath()` 基于实际加载的 avc.dll 路径推导，`plugins/avc.dll` 被 pyd 优先加载导致 assets 拼到 `plugins/assets/`（实际在 root/assets/），shader 加载失败段错误。修复：删掉 `Release/plugins/avc.dll`（pyd 沿 PATH 加载 root/avc.dll）+ `__init__.py` 预加载 root/avc.dll 兜底。⚠ 重新构建 avc 时注意不要再次把 avc.dll 复制进 plugins/。

## 进度（大方面）

| 阶段 | 内容 | 状态 |
|---|---|---|
| A | 场景识别 + 守护 + 对话 | ✅ 完成 |
| B | 导航（定位 + 传送 + 移动） | 🔧 定位通（SIFT 305ms）、传送链**已实机闭环**（panel=TELEPORT→点按钮→传送成功，7 项标定）；剩避 pin 路径专测 |
| C | 战斗（血条 + 连招 + Q） | ✅ 骨架完成 |
| D | P1 任务（boss + 地脉 + 树脂 + 每日奖励） | ✅ 骨架完成（claim_daily_reward + daily 总控 + 6 国冒险家协会路径 + 领邮件/合成树脂/尘歌壶/自动秘境） |
| E | AI 口子预留 | ⬜ 延后（等实机测试全过再开始，见 memory defer-ai-until-real-machine） |

测试：**335 passed / 0 failing / 2 skipped**（含传送确认修复新增 15 测 + 7 个既有漂移测试已同步到 2026-08-08 实机标定语义：position 轴对换 / drag scale 3.57 / M 复位 / switch_country v1 禁用）。实机 verify **21 项全 OK**（截图/场景/OCR/朝向/定位/地图标定 6 项）。

## 接手入口

- 实机诊断：`python main.py --task verify`（只读）；加 `do_teleport=true waypoint=七天神像-风` 测传送；加 `do_map_calib=true`（先按 M 开图）标定传送地图导航 6 常量。
- 代码：`src/framework`（核心：context·runtime·task·registry·sandbox·daemons/(9)·retry·stuck）；`src/abilities`（领域：detector·game_state·dialog·fighter·reward·daily·mail·craft·pot·domain·navigation/(tp·map_ops·position·camera·navigator·path_executor)）；`src/tasks`（插件：auto_boss·auto_ley_line·claim_daily_reward·claim_mail·craft_resin·enter_pot·auto_domain·daily·verify）。
- 资源：`resources/` 已提取 BGI 模板 + `tp.json` + 309 条路径 + 6 国冒险家协会路径 + ONNX 模型。

## 硬约束（容易踩坑）

- **定位坐标系**：BGI 翻转（`game_x = Left - px*zoom/Scale`，非相加）；坐标可为负，用 `_has_prev` 判初值，**勿用** `prev_x <= 0`。
- **DPI 缩放**：SourcePlayer 走 `enableSizeScale` + `enableSizeChange(1920,1080)` GPU 侧缩放；`IScreenCapture` 回退需裁剪+缩放。
- **SWIG `IImageBuffer`**：mask 系列返回包装类型，无 width/height，需 `Image.IImageBuffer(raw)` 再包一层。
- **拟人化是框架层**：avc 无 `setHumanize`，由 `utils.py` + `GameContext` 实现。
- **YOLO = 标准 Ultralytics**：别再做 sigmoid（已 bake）；`[1,4+nc,N]` 需转置；letterbox min 比例 + `(x-pad)/scale` 反变换。
- **分辨率任意**：框架自动把非 1080P 的截图缩放归一化到 1920×1080 输出（所有坐标基于 1080p）；原神以管理员运行；仅供技术研究学习。

## 近期变更

- **2026-08-08** 实机传送闭环 + 鼠标坐标修复（决定性根因，对应上条「下一步」）：**① 鼠标偏左上 bug 根因** —— 本进程 DPI-unaware（系统 DPI=96）→ `GetWindowRect/GetClientRect` 返回物理像素；而 avc `sc.toScreen` 用窗口 DPI（240）把坐标逻辑化（span 缩小 1/dpi），与 `ic.moveTo`/`getCursorPos`（物理像素）不同空间，导致传送到屏幕左上。修复：`context.py` 重写 `to_screen`（新增 `_window_geometry()` 每次现算 Win32 物理几何 `win_l/win_t/side_border/top_border/cli_w/cli_h` + 逻辑/物理 DPI 启发式判定，1080p 线性映射到客户区，绕开 `sc.toScreen`）+ `_calc_window_offsets` 不再二次乘 DPI（先 `_dpi_scale=dpi/96` 再存边框）。用 `cache/diag_cursor_probe.py` 探针验证：8 个测试点鼠标落点 vs Win32 客户区理论位置 **全部 ±1px**。**② 实机传送闭环**：`verify do_teleport=true waypoint=七天神像-风` 全链路成功——导航收敛（iter0 dist=544→iter1 dist=152）、4 个 Goddess 候选、点第一个 `@(1014,454)`→**`panel=TELEPORT`**→点传送按钮 `@(1689,1007)`→传送到 `(1854.05,-578.44)`（目标 1859,-576），15/15 verify 探针全 OK。当前问题#1 关闭。下一步：避 pin 路径专测（见顶部）。
- **2026-08-08** 修复传送确认（OCR 面板检测 + 避 pin，对应上条探查的下一步）：新增 `abilities/tp_panel.py`（`TeleportPanelKind` NONE/TELEPORT/MARKER + `detect_tp_panel` OCR 分类 + `find_teleport_button` + `close_marker_panel` 只按 Esc；⚠ 标记面板 '确认' 按钮带 F 快捷键，**绝不按 F**）；`map_ops.py` `find_tp_icon`→`find_tp_icons` 返回按距视口中心排序的候选列表；`tp.py` `_navigate_map_to_target` 返回候选、新增 `_click_and_confirm_teleport`（遍历候选→点击→OCR 检测→标记面板 Esc 关闭换下一候选 / 传送面板点按钮或按 F，超时换点）+ `_wait_and_confirm_teleport` 改 OCR；`daemons/quick_teleport.py` 同步改用 OCR 面板检测。测试：新增 `TestTpPanelDetect`(7) + `TestTeleportConfirmFlow`(8) 全通过；另把 7 个既有漂移测试同步到标定语义（position 轴对换、drag scale 3.0→3.57、tp.py M 复位循环、switch_country v1 禁用），全套 335 passed / 0 failing。
- **2026-08-08** 传送链实机探查（决定性根因）：点开的地图卡片是**自定义标记面板**（OCR '追踪'/'删除'/'标记（点击更改标记名称）'，玩家 114 个标记覆盖传送点），非传送面板。实验脚本在 `Temp/claude/`：`test_panel_seq.py`（点击图标后 OCR 全帧，证明标记面板）、`test_fkey.py`（点 Goddess@(1176,744)→面板有 F 提示→按 F→地图未关未传送）、`test_waypoint.py`（dump 全部传送图标匹配，点 TeleportWaypoint@(372,638) 无面板）、`diag_confirm.py`（GoTeleport.png 实机仅 0.588）。同时确认：`ctx.click_at` 内部走 to_screen 正确转换（`ctx.ic.click` 直接吃屏幕坐标勿用于 buffer 坐标）；`ctx.save_debug` 存原始窗口分辨率（3206×1876），调试图坐标需缩放约 1.67×/1.74× 才对应 1080p。下一步：修确认逻辑（OCR 面板检测 + 避 pin）见顶部「下一步」。
- **2026-08-07** BGI 对齐核查（B1-B6，`docs/design/08-BGI对齐核查.md`）：①秘境坐标改查 tp.json（BGI DomainPositionMap 同源，35 个可刷秘境，旧 4 硬编码坐标 3 错 1 无效，绝缘之境=椛染之庭别名）；②mail/craft/pot/domain 模板阈值统一 0.8（BGI 默认，规避 E1 见 0.71 误匹配）；③pot 4 模板补 BGI ROI（potLove/potMoney/potPageClose/pageCloseWhite，兼治 avc 全屏慢匹配）+ 退出先点 PageCloseWhite；④domain 确认按钮 btn_white_confirm→confirm.png（BGI AutoFight，右半 ROI）+ party_btn_choose_view ROI；⑤mail 开头加回主界面；⑥路径资源核对齐全（craft 4 国/boss/guild 6 国/ley_line 319 条）。测试 320 passed。
- **2026-08-07** 五方向推进（D 可靠性 + C 守护 + A 日常补齐 + B 自动秘境 + E 离线验证）：`framework/retry.py`（`retry_on` 指数退避装饰器 + `retry_until` 轮询，NormalEnd/CancelledError/PolicyViolation 不重试）+ `framework/stuck.py`（`StuckDetector` 连续 N 次同值判卡死）+ `daemons/auto_talk.py`（NPC F 交互，priority=5 高于 auto_pick）+ `abilities/mail.py`（领邮件 `claim_all_mail`）+ `abilities/craft.py`（合成浓缩树脂 `craft_condensed_resin`）+ `abilities/pot.py`（尘歌壶进入/领奖/退出）+ `abilities/domain.py`（自动秘境 enter/fight/claim/exit，4 秘境坐标）+ 4 任务插件（claim_mail/craft_resin/enter_pot/auto_domain）+ `daily.py` 总控补全（领邮件→合成树脂→自动秘境→首领→地脉→每日奖励→尘歌壶，步骤失败不阻断）+ `game_state.py` 新增 18 模板条目 + 18 个 `has_*` 原语；资源复制 12 模板（esc_mail_reward/craft_condensed_resin/sereniteapot_love 等）+ 4 合成台路径；修 `_resolve_template_path` 兼容 `res.template_ui()` 相对路径（避免二次拼接）；E1 `cache/verify_template_match.py` 960×540 降采样 + ROI 分组验证 20 新模板 6 张真实图**无误匹配 ≥0.8**（最高 0.71，avc 朴素模板匹配 1080p 全屏 ~13s/模板偏慢，实机需 ROI）。`tests/test_retry.py`(23) + `test_phase_daily_new.py`(22) 新增。测试 **320 passed / 0 failing / 2 skipped**。
- **2026-08-07** 每日奖励领取领域能力 + 任务插件 + 总控编排：`abilities/daily.py`（F1 冒险之证直领 `claim_encounter_points` + 凯瑟琳对话回退 `claim_daily_at_guild` + 派遣一键领取/重探 `one_key_expedition` + 总控 `claim_daily_reward`）+ `tasks/claim_daily_reward.py`（任务插件）+ `tasks/daily.py`（每日总控：auto_boss→auto_ley_line→claim_daily_reward，其余留 TODO）+ `resources/paths/guild/` 6 国冒险家协会路径（蒙德/璃月/稻妻/须弥/枫丹/挪德卡莱，从 BGI 复制）+ `tests/test_daily.py`（29 测：领域能力各函数 + 任务插件 + 总控编排 + Registry 可发现）。测试 269 passed。（B 首批）：avc 检测能力端到端离线验通——`cache/verify_yolo_detector.py`（bgi_world 7 类：health bar/enemy identify/drops/ore/unmined ore/chest/locked chest，**不含怪物类**，怪物走头顶血条检测；fight 图检出 health bar+ore，框 78×7 横条证 `[1,4+nc,N]` 转置对、center.y 落上半部证 letterbox 反变换对、分数 0.84/0.64/0.43 不饱和证未重做 sigmoid——**3 坑均未踩**）+ `cache/verify_ocr.py`（PP-OCRv6 认出"原粹树脂"/"地脉之花"等 score≥0.985，OCR path 修复后链路正常）。`test_web_screens` manifest 加 fight `yolo_min:{health bar:1}` + leyline `contains_text:[原粹树脂]`（detector+OCR 从"诊断打印"转真断言）。测试 240 passed。
- **2026-08-07** 朝向换算核对闭环：读 BGI 源码确认 `get_orientation` 与 `target_orientation` 同坐标系（BGI `RotateToApproach` 直接相减 + FromGia/PredictRotation/GetTargetOrientation 三同系），`camera.py` docstring 修正"待实机标定"→"同系无需换算"；新增 `tests/test_camera_orientation.py`（13 测：target_orientation≡BGI acos 公式 + `_angle_diff` + `_control_ratio`）。当前问题#3 关闭。
- **2026-08-07** avc 大地图 SIFT 定位对齐 BGI（底图特征预存 + 切块局部搜索）：avc `IFeatureMatcher` 加 3 方法（`loadTrainFeaturesPath`/`matchQueryLocal`/`matchQueryFull`，复刻 BGI `.kp.bin`/`.mat.png` 预存 + 60×88 切块 + FLANN KnnMatchLocal/Match），`position.py` 改用新接口（删 addReference/match 全图重算、加 `_build_search_rect` + `_load_train_features`），`tp.py` 传 `expected_center`（首轮全图、后续切块局部）。测试 226 passed。
- **2026-08-07** 传送地图导航实现：`position.py` SIFT 视口重定位（`get_position_from_big_map`）+ 新建 `map_ops.py`（`MapController`：zoom 测量/滚轮缩放/拖拽/地面层切换/7 国切换/图标匹配）+ `tp.py` 重写 `_navigate_map_to_target`（MoveMapToCore 循环）+ `_wait_and_confirm_teleport` 模板匹配替代硬编码 + `path_executor.py` 传送后锚定 navigator prev + 修 `test_readonly_probes`（MockPos 补 `set_prev_position`）；`verify` 加 `do_map_calib` 6 项标定探测。测试 217 passed。
- **2026-08-06** 实机首测 + 定位修通（DPI 缩放 / 坐标翻转 / MapMatcher NaN / SWIG 包装修复）。
- **2026-08-08** 实机标定传送地图导航 6 项常量（`verify do_map_calib=true`，21 项全 OK）：滚轮方向/每槽步长（`_ZOOM_WHEEL_SIGN=-1`/`_ZOOM_PER_NOTCH=0.083`，实测 scroll(0,+1)×5→delta=-0.417；⚠ 大槽数单次 scroll 被游戏吞掉，必须逐槽）、拖拽方向（X/Y sign=1）、`_MAP_SCALE_FACTOR` 2.361→3.0（BGI 值 SendInput 物理像素语义，avc moveTo 逻辑像素需 dpi 补偿）、坐标轴对换正确。新增 F9 全局取消热键（`framework/hotkey.py`：RegisterHotKey + GetAsyncKeyState 双保险，非管理员/占用时轮询兜底；Runtime `_start_hotkey`/`_stop_hotkey` 集成）+ 单行状态显示（`framework/status.py`：verify 改为覆盖式单行 + 5 秒自动清除）。修 avc shader 路径 bug（见当前问题#7）：删 `plugins/avc.dll` + `__init__.py` 预加载 root/avc.dll。测试 308 passed（phase_b 92 个含新增标定断言）。下一步：实测传送闭环。
- **2026-08-05** 阶段 A–D 骨架完成。
- **2026-08-04** 框架地基（context / runtime / @task / sandbox / game_state）。
