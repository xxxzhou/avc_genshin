# 任务进度

> **开机文档**。读 `CLAUDE.md` + 本文件即可接手。设计见 `设计实现.md` + `docs/design/`。
> 维护原则：强调「下一步」与「待实机项」；已完成仅留一行大事记；踩坑/标定压到「事实备忘」。

## 下一步（当前主目标）—— 打通走路段

**2026-08-15 实机夜**（详见 `logs/real_machine_2026-08-15.md`）：传送链跨区修通（4 个叠加 bug）、死亡检测+复活实机闭环、走路遇敌战斗接入、GuardRail 取消协作化、**可观测性验收 5/5 通过**（①选远②卡③复活④吃药⑤怪检均可按 ability/stage/reason 归因）。

**2026-08-15 上午实机验证（r_20260815_064010，craft_resin 璃月）——揪出新 P0：截图管道冻结**。传送确认点击实际**成功**（save_debug 的 sc 原生帧证明 ~20s 已到璃月主界面），但 SourcePlayer（高频截图）冻结在传送前的旧地图帧 80s+ → scene_estimator 一直判 map → `wait_main_ui` 空等 60s 超时（还被无视）→ 走路段在旧帧上读小地图 → 假定位 [3201,-967] 锁死 → too_far 盲走 → 任务失败。**三层修复（已写，+9 测，待全量回归+实机复跑）**：
1. **capture() 冻结守卫**（context.py，根治）：SourcePlayer 帧指纹（中心 96² md5）>6s 不变 → 本次走 IScreenCapture 回退（sc 实测始终真实）；`capture.stale` 事件上报冻结/恢复。
2. **teleport() 归因**（tp.py）：`wait_main_ui` 超时 → `TaskError("传送后 60s 未回到主界面")` + `tp.confirm reason=wait_main_ui_timeout`，不再盲继续。
3. **position 场景守卫**（position.py）：scene=map 时小地图定位直接 `reason=scene_map_minimap_invalid` 判失败（旧帧伪小地图曾匹配出跨 4000 单位假位置）。

**cam.rotate 已双管齐下（待实机验证）**：
1. **架构**（navigator.py）：`_REORIENT_THRESHOLD` 45→90——停步式 `rotate_to` 仅严重反向才触发，其余由移动中 `rotate_camera_to_target`（diag_moveto 已证收敛）边走边转；卡住恢复的 180° 反转改 `max_attempts=4` 轻量尝试、失败不阻塞。
2. **失速检测**（camera.py `rotate_to`，commit 0db8d4c）：角色被地形挡住时 nudge W 无效 → 面朝读数不变（实测 +300px×5 → Δ≈0°）→ 原先 max_attempts 空转 30s+。现连续 2 轮大位移后面朝变化 <1.5° 且未收敛 → 快速中止 `reason=stalled`（~6s），交给走路段收敛；进展轮清零 counter；光标每轮回中防边缘裁剪误判。
3. **传感器换路已排除**：HUD 无罗盘、小地图固定北朝上、大地图无朝向指示——相机偏航无独立传感器，只能靠角色面朝（箭头）间接读，修 nudge 空转是唯一可行路径。
**px→角度映射非线性仍存**（同 +600px 在 +4°~+131° 波动）：自适应增益在线修正已加；若实机仍慢，方向是标定不同 DPI/窗口状态映射曲线。

**2026-08-15 上午实机战果（本次会话推进）**：
- ✅ **截图冻结三层修复全部实机验收**：#1 capture() 冻结守卫（r_070828 两次冻结→恢复）、#2 wait_main_ui 归因（r_071706 实机完整触发）、#3 position 场景守卫。
- ✅ **craft_resin 璃月全链触达**（r_070828 传送+落地云来海+走路段；r_071044 死亡+复苏闭环）——失败根因转为 **fighter 战斗力**（合成台附近被冰史莱姆打死），非截图冻结。
- ✅ **新 P0 修复（本次新增）——avc 内部 OpenCV `cv::crossCorr` 断言崩溃**（模板≥搜索区域时 `matchTemplate` 触发，走 terminate 绕过 Python 异常路径→0 存证）。三层防御：①`vision_utils._template_fits` 模板尺寸守卫（find_template/find_all_templates）；②`game_state._find_template` 同守卫（scene_estimator 10Hz 轮询）；③`context.capture()` 坏帧尺寸守卫（SourcePlayer 回读尺寸≠1080p→丢 IScreenCapture 归一化）。+6 测。**多任务实机复跑不再崩溃**（claim_daily_reward/craft_resin 均干净归因失败）。
- ❌ **新 P1（待实机迭代）——大地图 SIFT 拖拽导航卡死**：`tp.navigate` 时 `pos.bigmap` SIFT 定位准确（verify 确认 [2000,-868]，凯瑟琳 [2278,-876] dist=278），但 `drag_map` 手势后 SIFT 视口中心不更新 → `nav_abort_no_progress` 死循环。当前 zoom≈4.44 工作档、拖拽方向正确、位移合理（223px），但视口没动。**怀疑 `_drag_gesture` 起点 (1440,60) 落在大地图顶部 UI/标签上拦截拖拽**（代码注释已列实测可用起点 (1500,800) 等，顶部 60px 未验证）。方向：实机标定拖拽起点避开顶部 UI，或拖拽前强制 `set_zoom_level` 到稳定档。

**下一步**：①提交本轮改动（截图冻结三层修复 + P0 OpenCV 守卫 + 测试）；②实机迭代 SIFT 拖拽起点（P1，打通大地图导航）；③fighter 战斗力（走路段被冰史莱姆打死）；④auto_ley_line v2 待走路段打通后跑。

**2026-08-15 下午实机推进战果（拖拽打通跨区传送 + 落地判定修复）**：
- ✅ **跨区传送彻底打通（重大突破）**：`tp.navigate ok dist=62` 蒙德→璃月云来海合成台收敛；`tp.confirm panel=TELEPORT` 传送面板确认。**根因**：`map_ops._drag_gesture` 起点按方向自适应公式 `_MAP_CENTER_X - sign_x*480` 在纯西向反向时生成 (1440,540)，落在大地图右侧 UI → mouseDown 被吞 → Δwest=0。**修复**：改用固定安全起点 `_DRAG_START_SAFE=(1100,540)`（已标定 80%+ 像素差）。实机 `verify do_map_calib` 复测 `drag(west-200)` Δwest 从 0→-204（之前 0）。+1 测。
- ✅ **传送落地判定修复（_force_scene_refresh）**：`tp.py teleport_to` 在 `_click_and_confirm_teleport` 后、`wait_main_ui` 前强制 capture+classify_scene 写入 shared.scene。**根因**：传送期间 SourcePlayer 冻结触发 capture.stale，scene_estimator 守护用 IScreenCapture 回退帧在过渡瞬间判 MAP/UNKNOWN，shared.scene 滞后 → wait_main_ui 60s 等不到 MAIN_UI 误判失败（实机 go_teleport_detected.png 证明玩家已落地主界面）。
- ❌ **走路段 avc MapMatcher `cv::crossCorr` 崩溃（avplay 底层，无法在当前 Windows 环境根治）**：
  - 崩溃精确栈：`OpenCV(4.13.0) Error: Assertion failed (corr.rows <= img.rows + templ.rows - 1 && corr.cols <= img.cols + templ.cols - 1) in cv::crossCorr, file templmatch.cpp line 588`。
  - 定位到 `D:\avc_godot_pack\plugins\avc\__init__.py`（avc Python 绑定）→ `avplay/plugins/avc_opencv/MapMatcher.cpp` 的 `exactMatch`/`roughMatch` 的 `cv::matchTemplate`。`roughMatch` 在 282-285 行有 `coarseSize > searchRoi` 防护但**未覆盖所有边界分支**（autoRoi 回退、maskF 通道）；`exactMatch` 382-385 行有防护但**模板 exactSize=260 vs 实际 mini156(156)** + searchRadius 默认值的边缘组合可能触崩。
  - **当前 Windows 环境无 cmake/MSVC/ninja，无法重编 avc**——需用户在装了 MSVC + cmake 的环境跑 `python D:\Work\github\avplay\build_windows.py` 重编 avc_opencv 插件 dll 并更新 Godot 包。
  - **建议 avplay 修复**（最小改动）：在 `MapMatcher.cpp` 的 `match()` 入口加**统一 matchTemplate 模板≤搜索区域 + mini≤img 双重断言**，不满足直接 `lastError` 返回 0（不崩溃）。同样适用于 `plugins/avc_opencv/TemplateMatcher.cpp`。
  - **Python 侧已无法进一步防护**：avc terminate 绕过 Python 异常路径，三层守卫（`_template_fits` / `_find_template` 守卫 / capture 坏帧尺寸守卫）+ `_force_scene_refresh` 都已加，仍无法 catch avc 内部 assert 崩溃。
- **craft_resin 璃月实机当前状态**：跨区传送全链成功（蒙德→璃月云来海），走路段最后几步撞 avc MapMatcher 崩溃。距离合成台仅 dist=62（约 5-10 步行），走完即能合成。**核心阻断 = avc 走路段崩**，治本 = 重编 avplay。

**2026-08-15 下午 fighter 链路实机诊断（r_20260815_073402，diag_fight）—— 能打怪/检测血量**：
- ✅ **fighter 全部底层实机工作**：玩家当前蒙德城内 [1982,-866]（被房子挡，无怪视野），diag_fight 不依赖走路/传送，直接跑 fighter 链路：
  - `detect.blood` ×6+ → `has_enemy=False`（**血条 IColorDetector 实机工作**，正确返回无怪）
  - `seek_enemy` ×8 → `fight.enemy not_found_after_seek`（**索敌转视角实机工作**，无怪→预期失败）
  - `bgi_world.onnx` ONNX 模型实机加载（世界敌人 YOLO，含发呆态）
  - `q_classify_sim.onnx` 实机加载（Q 就绪分类）
  - `avatar_side_classify_sim.onnx` 实机加载 + `detect.classify ok score=1.00`（出战角色识别置信度 1.0 → "Odette"）
- ✅ **血量/死亡/回复检测实机工作**：`is_low_hp=False`、`has_resurrection_icon=False`、`has_recovery_icon=False`（当前正常血所以全 False，**检测路径调用成功无崩溃**）；auto_eat 守护已挂载（150ms 写入 shared.low_hp）。
- ❌ **fight_cleared=None**：当前视野无怪（蒙德城内），无法演示实际打怪。**要实机看打怪演示需走到开阔怪点（蒙德城外风起地/星落湖，或跨区到璃月/须弥——但 SIFT 大地图导航 P1 拖拽卡死未修）**。
- ❌ **能打地脉花吗？** `auto_ley_line` 任务代码完整（找花→传送→战斗→领奖→循环），但同样依赖大地图 SIFT 导航（找花+传送），**P1 不修就跑不通**。
- **关键瓶颈归纳**：fighter 全链路已可实机打怪，但**没有在蒙德地区或蒙德本地怪点的实机入口**（48 个 boss 路径都在璃月/稻妻/须弥/枫丹/挪德卡莱；craft 任务璃月有，但合成台附近被冰史莱姆打死）。要打通任意一个战斗任务都必须**先修 P1 大地图 SIFT 拖拽卡死**。

**avplay 底层**（D:\Work\github\avplay）：avc C++17 SDK。血条检测下沉到 `avc_opencv` 的 `IColorDetector`（BGR 精确匹配 + 连通域）；战斗模型走 `avc_onnx`（YOLO + 分类）。fighter.py docstring 已确认：血条是「战斗专属可靠信号」（`AvatarRecognition.FindBloodBars` 对齐 BGI）；世界敌人走 `bgi_world.onnx` "enemy identify" 类（含发呆态）；Q 就绪走 `q_classify_sim.onnx`（ROI 右下）。底层能力完备，关键在**上层跑通路径**。

## 状态速览

| 阶段 | 内容 | 状态 |
|---|---|---|
| A | 场景识别 + 守护 + 对话 | ✅ |
| B | 导航（定位+传送+移动） | 🔧 传送链/避 pin/go_to/边走边锚定已实机闭环；path_executor 整段 + 小地图冷启动纳塔歧义待实测 |
| C | 战斗（血条+连招+Q+生存） | 🔧 骨架完成；生存改进待实机 |
| D | P1 任务（boss+地脉+树脂+日常） | 🔧 骨架完成；流程待实机 |
| E | AI 口子 | ⬜ 延后（实机全过再开，见 memory defer-ai-until-real-machine） |

测试 402 passed / 2 skipped（0db8d4c 全绿）；截图冻结三层修复 +9 测单文件已过、**全量回归待跑**。verify 实机 14 OK/0 ERR（2026-08-14 夜复测）。

## 待实机项（主目标之外）

- `path_executor`：一条路径（传送→多 waypoint 分段 go_to）整段跑通（`cache/diag_path.py`）。
- 小地图定位冷启动全局匹配：纳塔地形自相似歧义（真 0.954 vs 假 0.959）；正常流程 prev 由传送落点 seed，局部锚定不受影响。
- D 各任务流程（auto_boss/auto_ley_line/auto_domain/claim_mail/craft_resin/enter_pot）实机。
- 战斗生存（红血→吃药/换人、死亡→传送最近神像复活）实机。
- avc 模板匹配偏慢（朴素标量，1080p 全屏 ~13s/模板）；实机各任务都带 ROI，暂不阻塞，全流程跑通后再优化。

## 实机诊断 SOP（claude code 自驱 = 眼睛+大脑）

实机测试时 AI 不靠用户转述画面，自己跑→看→定位（2026-08-12 闭环已建）：

1. **跑任务带可观测性**：`python main.py --task <name>`；挂 `timeline_snap` 存中段画面
   （`@task(daemons=[...,"timeline_snap"])` 或 `ctx.mount("timeline_snap")`），失败自动存 evidence。
2. **汇总诊断包**：`python -m framework.diagnose <run_id>` → 一屏看 task / 失败 ability 分组 /
   scene 时序 / 事件时序 / 可读截图清单（evidence + timeline）。
3. **看画面**：Read `debug/<run_id>/*.png`（evidence=失败那一刻）+ `timeline/*.png`（周期回放找卡点）。
4. **定位**：diagnose 点名的失败 ability 的 stage/reason + 画面判读 → 坏在哪个 ability 哪个决策点。
5. **改 + 重跑**：改代码 → 重跑 → diagnose 验证失败消失。

素材入口：verify `snapshot` 探针（存当前帧）/ evidence（失败存证）/ timeline（周期回放）。
不调外部 API，判读由 claude code 本体（Read 截图）完成。

## 接手入口

- 实机诊断：`python main.py --task verify`（只读）；`do_teleport=true waypoint=...` 测传送；`do_map_calib=true`（先按 M）标定 6 常量；逐功能探针 `probe=... group=... confirm_action=...`（**先 probe 定位，别跑整任务**）。
- 代码：`src/framework`（context·runtime·task·registry·sandbox·daemons·retry·stuck·observe·logging）；`src/abilities`（detector·game_state·dialog·fighter·reward·daily·mail·craft·pot·domain·navigation/[tp·map_ops·position·camera·navigator·path_executor·arrow·trap_escaper]）；`src/tasks`（插件）。

## 硬约束 / 事实备忘（踩过的坑，勿重踩）

- **截图走 `ctx.capture()`**：SourcePlayer GPU 缩放到 1080p；直 `sc.getBuffer()` 是原始分辨率（如 3206×1876），坐标全错。**鼠标坐标走 `ctx.to_screen`**（自算 Win32 物理几何，绕开 DPI-aware 不一致）。
- **坐标系**：BGI 翻转 `game_x=Left-px*zoom/Scale`；坐标可负，用 `_has_prev` 判初值，**勿用** `prev<=0`。统一轴序 (北,西)=`position[0]/position[2]`。
- **原神不能原地转身**：旋转靠轻推 W 走路，大角度累计位移会撞地形；导航层旋转后须重定位/选开阔地。
- **传送地图导航常量**（2026-08-08 实机标定）：`_ZOOM_WHEEL_SIGN=-1`/`_ZOOM_PER_NOTCH=0.083`/`_DRAG_*_SIGN=1`/`_MAP_SCALE_FACTOR=3.0`（avc 逻辑像素，非 BGI 2.361）/`_MAP_DRAG_START=(960,650)`（不能在中心，玩家标记拦截中心拖拽）。
- **拟人化是框架层**：avc 无 `setHumanize`，`utils.py` 实现。**YOLO=标准 Ultralytics**：别再 sigmoid/`[1,4+nc,N]` 需转置/letterbox 反变换。类名**带空格** `"enemy identify"`。
- **avc 重建后**：`touch common.i` + build `avc_swig_python`（非 AvcWrapper）才更新 Python 绑定；别把 avc.dll 复制进 plugins/。

## 已完成大事记（一行/项）

- **2026-08-15 上午 craft_resin 璃月实机（r_20260815_070828）—— 传送+落地+走路段全链实机打通**：
  - ✅ **capture() 冻结守卫实机生效**（修复 #1 验收）：13.77s 冻结→60.64s 恢复、67.36s 冻结→69.56s 恢复，两次触发均回退 IScreenCapture，`capture.stale` 事件正确上报。
  - ✅ **传送链**：`tp.resolve target=(-653,267)` → 大图拖拽 dist 7821→30 收敛 → `tp.confirm pin_blocking ×2`（自动换候选）→ 第 3 候选面板 TELEPORT 确认 → 落地璃月云来海（`0004_73.4_main_ui.png` 截图证明）。
  - ✅ **path_executor 走路段**：`path.segment ok` 后 `nav.step mode=walk dist=385→334→396` 连续推进，pos.match `mode=local` 锚定稳定（修复 #3 守卫未触发伪定位）。
  - ✅ **survival.low_hp**：82s 检测到低血，auto_eat 兜底。
  - ❌ **新 P0（avc 内部，非我们代码）**：82s 后走路段某次 `find_template` 调用触发 OpenCV `cv::crossCorr` 断言 `corr.rows > img.rows + templ.rows - 1`（`matchTemplate` 模板 > 搜索区域）。崩溃走 OpenCV `terminate handler`，**绕过 failure 路径** → 0 evidence 存证、observability 无法归因。方向：①在 `GameContext.capture()`/模板匹配调用处加 `cv2.setExceptionMode` 或 try/except；②ROI 钳边校验；③path_executor 在 `find_template` 前验模板 < buffer。
  - 副产物：`craft_resin` `country=蒙德` 也会执行——但 `resources/paths/craft/` 只有 `合成台_璃月.json`，蒙德路径不存在时直接 `return False`（需 country→路径校验/兜底）。
- **2026-08-15 上午 craft_resin 璃月第二次（r_20260815_071044）—— 全链触达 + death/revive 闭环**：
  - ✅ **跳过传送**（玩家已在璃月云来海，传送链已就绪；`tp.navigate not_in_map` 误判后 `path.segment ok` 继续走路段）。
  - ✅ **nav.revive ok** 死亡检测+复苏闭环再次实机触发（`0001_16.5_main_ui.png` 死亡「复苏」画面被 timeline 捕获）。
  - ✅ **走路段+战斗**：玩家路径执行途中被冰史莱姆围攻 → 全队阵亡 → pos.match no_match × 6 → `nav.revive ok` → 任务归因中止（`'走路段全队阵亡：已点复苏复活（回到最近神像），任务中止重跑'`）。
  - ✅ **craft_resin 璃月全链 = 稳**：传送链+path_executor 走路段+死亡检测+复苏+任务归因——**核心链路全部实机触达**。崩溃归因不再是「截图冻结伪定位」而是 **fighter 战斗力**（任务进度已知遗留 #2）。
  - 下一步非阻断：① fighter 战斗力（auto_boss 战斗段存活率）；② auto_ley_line v2 待 craft_resin 闭链后跑；③ 新发现 P0（avc OpenCV terminate 绕过 failure 路径）——给模板匹配调用加 try/except + ROI 钳边。

- **2026-08-15 实机夜（AI 自驱 8h）**：传送链跨区修通（拖拽分段≤400px/手势 + 起点自适应避标记 hitbox + 鼠标残留态清理 + dist 无进展复位 + SIFT seed + z≥5.7 概览档禁入）；死亡检测+复活实机闭环（Resurrection 模板实机裁剪 + navigator pos_fail→查复苏→点按钮→归因中止）；`ctx.check_cancel()` 取消协作化（auto_kill 后不再空转）；path_executor 走路遇敌 `_fight_if_in_combat`；enter_pot 重写背包流程（壶异地放置留 TODO）；cam.rotate 自适应增益；**可观测性验收 5/5**。测试 397 passed。详见 `logs/real_machine_2026-08-15.md`。

- **2026-08-12 实机诊断闭环**（离线就绪，无实机时做的准备）：`python -m framework.diagnose <run_id>`（读 jsonl → 汇总诊断包：task / 失败 ability 分组 / scene 时序 / 事件时序 / 可读截图清单）+ `timeline_snap` 守护（周期 + scene 跳变存中段画面到 `debug/<run_id>/timeline/`，填「卡在中段无 evidence」盲区）+ verify `snapshot` 探针（存当前帧）。判读由 claude code 本体 Read 截图完成，**零外部 API / api_key / 模型配置**。+14 测（8 diagnose + 6 timeline）。详见上方「实机诊断 SOP」。
- **2026-08-12 实机夜**：GuardRail 三层白名单 + auto_ley_line v2 实机 + 4 任务验收 + subagent 诊断 P0/P1。**修复 11 处**：①`guardrail.IGNORE_REASONS`={template_not_matched,no_blood_bar}（same_reason+same_fail 同步跳过）；②reason=None 不算 same_reason 死循环（pos.match/nav.step 普通走步 reason=None 不再误杀）；③`detect.ui` 失败填 `reason=template_not_matched`；④`detect.blood` 无血条填 `reason=no_blood_bar`；⑤`find_blossom_and_nearest_tp` 补 5 处可观测事件 + 失败存图（previously 静默返 None）；⑥`find_nearest` 过滤 Domain 类型（铭记之谷不再被选成花的 nearest_tp）；⑦`trap_escaper` hold=0.1→2.0/1.0s（关键 bug：测试遗留的临时值，按 0.1s 按键角色完全不动）；⑧navigator 加 `walking_away` 检测（dist 连续上升 5 次 → 反向挣脱）；⑨**P0** `position.py` pos.match first-match-wins 加合理性检查（local 失败回退全局时距 prev > 1000 要求 score > 0.95，避免 MapBack_0 假阳性锁死）；⑩**P1** `arrow.py` _sym_heading 要求显著宽度差（w_min < w_max * 0.7，避免 180 度歧义噪声翻转）；⑪`camera.py rotate_to` max_attempts 加最小下限 8（避免入口 diff 小时 max_attempts=5 不够循环噪声收敛）。**实机**：verify probe=timeline 13 OK/0 ERR；verify do_teleport=true 闭环；claim_mail ✅；claim_daily_reward ✅；craft_resin 修后 cam.rotate 多次 ok reason=converged + trap.stuck ok（脱困成功），但仍卡在 path_executor 多段未集成。**未跑通**：auto_ley_line v2（path_executor 多 waypoint 未集成）；craft_resin（同）；auto_boss（tp.navigate drag_map 不生效）；enter_pot（设计 bug：尘歌壶应用背包小道具）。加 7 测（5 GuardRail 白名单 + 2 navigator walking_away）。pos.match 加 `mode=local_fallback_global` + `prev_distance` 字段。381 测全过。**详见** `logs/real_machine_2026-08-12.md`。
- **2026-08-11**：能力可观测性全量落地（**绑定规则**，见 `设计实现.md §2`）——`_NullObserve` no-op shim + `ctx.observe`/`g.observe` 访问器（永可调用，不判空）；Observe 加订阅者钩子 + 节流门（`_quiet`/`_transition`/`throttle_key`，`ok=False` 永不节流）；新增 `framework/report.py` 读端（`live_line`+`summarize`）+ runtime 打印订阅者 + teardown `run_summary`（按 ability 分组）；Tier-1 五咽喉（vision_utils/game_state/fighter blood+classify）+ Tier-2 决策（tp/navigator/position/camera/trap_escaper/path_executor[修 go_to 丢弃]/reward/fighter survival+revive+enemy+burst/tp_panel/map_ops）+ 7 个 `src/tasks` 任务级事件全插桩；`tests/test_observability.py` 16 测。下一步=实机 auto_ley_line 验收 AI 定位。
- **2026-08-08**：传送链实机闭环（DPI 修复 + OCR 面板确认 + 避 pin + 远距拖拽 9600 单位）；小地图定位轴序 bug 修复；go_to 实机闭环（真罗盘帧 + 持续按 W 边走边转相机）；朝向改自研箭头对称轴法（avc get_orientation 不可用）；生存系统改进（auto_eat 150ms + Z 通道 + fighter 生存检查 + 最近神像复活）；世界敌人识别补进 fighter（类名空格 bug 定论，live 闭环 s5 敌×3）。
- **2026-08-07**：BGI 对齐核查 B1-B6（秘境坐标查 tp.json / 阈值 0.8 / ROI / 坐标改 tp.json）；retry/stuck/auto_talk + mail/craft/pot/domain + daily 总控；avc 大地图 SIFT 切块对齐 BGI；传送地图导航 6 常量标定；F9 取消热键 + 单行状态。
- **2026-08-06**：实机首测 + 定位修通（DPI 缩放 / 坐标翻转 / MapMatcher NaN / SWIG 包装）。
- **2026-08-05**：阶段 A–D 骨架完成。**2026-08-04**：框架地基（context / runtime / @task / sandbox / game_state）。
