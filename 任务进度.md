# 任务进度

> **给新会话的开机文档**。读完本文件 + `CLAUDE.md` 即可接手。
> 维护规则：每完成一个阶段/任务,①更新下方状态表 ②在「变更日志」追加一条 ③控制在 ~100 行。
> 详细设计见 `设计实现.md` + `docs/design/`,真实进度以本文件为准。

## 一句话现状

机制测试 **196 项全绿**（合成画面，证明机制通）。真实画面测试已备好：`tasks/verify`（实机实时诊断）+ `tests/test_real_screens.py`（截图黄金断言），见下方「实机测试指南」。三个 CV 全下沉 avc（血条/定位/朝向，含 BGI 忠实移植）。**下一站：有游戏的机器上按指南做实机标定**（血条色域/定位缩放/朝向约定/OCR 文案）。

## avc 待办（明天处理）

- **`loadImagePath`/`setMapImageByPath`/`setFineMapImageByPath` 不支持 WEBP**：stb_image 只支持 BMP/PNG/JPG/TGA，不支持 WEBP。当前 workaround 是 Python 侧用 cv2 加载 WEBP → IImageBuffer → `setMapImage`/`setFineMapImage`。**应在 avc C++ 侧修复**：`loadImagePath` 遇到不支持的格式时，回退用 OpenCV（cv::imread）加载，这样 `setMapImageByPath`/`setFineMapImageByPath` 直接传 WEBP 路径即可。

## 方向调整说明

原计划阶段五是 AI 协作层(规划器/代码生成/缓存),用户决定暂缓——AI 客户端设计未定,先把 BetterGI 同等功能做出来。AI 层只留 `g.decide`/`Planner`/`TaskGenerator` 接口口子,不实现。

## 新阶段路线(A→B→C→D→E,按依赖顺序)

| 阶段 | 内容 | 解锁什么 | 状态 |
|---|---|---|---|
| **A** | 场景识别 + 资源对接 + 守护真实化 + 对话能力 | 守护能真正激活;`g.talk/talk_skip` 可用 | ✅ 完成 |
| **B** | 导航(PathExecutor + 小地图定位 + 传送) | `g.teleport_to/go_to` 可用;任务能"走到地方" | ✅ 骨架完成 |
| **C** | 战斗(SimpleFighter + 角色识别 + CD检测) | `g.has_enemy/find_nearest_enemy` 真实化;打怪任务能跑 | ✅ 骨架完成 |
| **D** | P1 任务(auto_boss/auto_ley_line 已建;domain/wood/fishing 卡资源暂缓) | 真实游戏自动化 | ✅ 骨架完成 |
| **E** | AI 口子预留(Planner/TaskGenerator Protocol + 失败回流 hook) | AI 大模型可接入的接缝 | ⬜ |

依赖链：A 是一切基础(没场景识别守护不激活)→ B 解锁移动 → C 解锁战斗 → D 才能写真实任务。E 贯穿始终。

## 阶段 B 关键信息(导航)

- **已完成**: tp.py(TpDatabase+Teleporter)、path_executor.py(Waypoint+PathTask+PathExecutor)、camera.py(CameraControl)、position.py(PositionGetter)、trap_escaper.py(TrapEscaper)、navigator.py(Navigator)
- **g.teleport_to/go_to 已接线**: 不再 NotImplementedError
- **小地图定位(已切 BGI 模板匹配,合成验证中)**: 已从 SIFT 切换到 BGI 模板匹配方案（`IMapMatcher`：朝向去旋转+粗匹配52×52彩图+精匹配260×260灰度图）。avc C++ 侧 `MapMatcher.cpp` 实现完整链路，修复 3 个关键 bug（①imageBufferToBgr 无限递归 ②matchTemplate 通道数不匹配 ③精匹配坐标缩放 coarse→fine 5x）。合成测试：粗匹配定位准确，精匹配 score=0.94，误差约 500 游戏单位内。⚠ WEBP 加载需 workaround（stb_image 不支持，Python 侧 cv2→IImageBuffer；**avc 待修：loadImagePath 加 OpenCV 回退**）。测试 2 项仍失败（position.py `_get_map_matcher` 流程需排查）。debug fprintf 待清理。
- **摄像机朝向(下沉 avc,BGI 忠实移植)**: `camera.get_orientation` 已切 avc `IOrientationDetector`(小地图缓冲直传;BGI CameraOrientationFromGia 峰卷积忠实移植,**先修了旧简化实现 warpPolar 几何 bug**——实测 warpPolar 输出 行=角度/列=半径)。无 avc 回退 Python `compute_orientation`(同算法)。3 项忠实性测试(噪声/箭头/梯度,Python vs avc 角度完全一致)。输出为 BGI 原始约定(0=右/东顺时针),与 `target_orientation`(0=北)换算待实机标定。
- **小地图常量已修正**: 从错误的 (24,12,280,280) 修正为 BGI 实际值 (62,19,212,212)（对照 MapAssets.MimiMapRect1080P）
- **BGI 路径 JSON 已复制**: 48 个 boss + 261 个地脉异常路径

## 阶段 C 关键信息(战斗) — 骨架完成

- **已完成**: `fighter.py`(SimpleFighter)；has_enemy/find_nearest_enemy(血条色块)、is_q_ready(q_classify 分类)、current_avatar(出战槽+avatar_side)、switch_character/fight/fight_until_clear；模块级 detect_blood_bars/has_enemy_in_frame(供 game_state 用)。
- **接线**: `g.has_enemy`(改血条,不再读 shared.detections)/find_nearest_enemy/is_q_ready/fight/fight_until_clear；`game_state` make_classifier 补 Scene.COMBAT。
- **敌人检测 = 红色血条色块**: cv2.inRange RGB(255,90,90)+connectedComponents,ROI(0,0,1500,900)排除 x≤200。**不依赖** bgi_world YOLO 类别(是否含敌人未验证)。
- **异常安全**: fight finally 三步释放(mouseUp 鼠标 + release_all_keys + 补 keyUp num1-4/x/z),补 release_all_keys「不释放鼠标 / 不含 num 键」两个坑。
- **测试**: `tests/test_phase_c.py` 30 项全过(血条检测/角色槽/派发/退出/释放 + avc 按键序列)。
- **真实游戏待验证**: ① q_classify/avatar_side 置信度(`_classify_crop` 用精确 resize 非 letterbox,需实测)② 血条检测在实机画面的准确度。无游戏环境,仅单测过。
- **未做(后续)**: auto_fight 守护、按角色名匹配连招脚本、E CD OCR、索敌转视角、联机侧栏。

## 阶段 D 关键信息(P1 任务) — 骨架完成

- **已完成**: `tasks/auto_boss.py`(世界首领:路径→战斗→领奖→循环,40 原粹树脂) + `tasks/auto_ley_line.py`(地脉之花:按 region 挑路径→激活→战斗→领奖→循环) + 共享 `abilities/reward.py`(`claim_resin_reward`:OCR 点『使用原粹树脂』直到消失/『补充原粹树脂』=耗尽返回 False/ESC 关页回主界面)。
- **骨架边界**: ① **位置追踪已有骨架**(avc `IFeatureMatcher` + `Teyvat_0_256.png`,合成数据验证,实机精度待标定)——PathExecutor 理论上能走了,但真实帧未验证;② auto_pick 与 auto_eat 同为 INTERACT 通道会 InputConflict,任务暂选 auto_eat 保命,掉物拾取未做;③ 未做树脂预检/脆弱树脂补充/回归七天神像;地脉按 region 挑第一条路径=固定点位。
- **测试**: `tests/test_phase_d.py` 9 项全过(树脂领取 3 情形 + 任务 happy/耗尽/缺资源 + Registry 可发现);全套 143 passed + 8 项 tp.json 缺失(环境,非回归)。

## 已完成:交付要点(找代码的入口)

**一·基础链路** — `context.py`(GameContext:懒导 avc/capture/坐标转换)、`detector.py`(GenshinDetector,onnxruntime)、`vision_utils.py`(模板/OCR 包装)、`_proto.py`(原型探针)。
**二·操作闭环** — 拟人化在 `utils.py`+`GameContext.click_at`,avc 绑定无拟人化故框架自实现。
**三·Runtime+地基** — `runtime.py`(asyncio loop+同步桥+守护框架驱动循环)、`daemons/`(8个)、`authority.py`/`observe.py`/`policy.py`/`scene.py`/`shared.py`/`cancellation.py`。
**四·任务体系** — `task.py`(@task)、`sandbox.py`(AST 白名单)、`registry.py`(discover/热加载)、Runtime.`run_task/_run_inline`;示例 `tasks/ping.py`、`compose_ping.py`;CLI `--task`。
**A·场景+守护+对话** — `game_state.py`(make_classifier+12个特征原语)、`dialog.py`(talk/talk_skip/visible_options)、守护真实化(auto_skip+auto_pick升级)+3新守护(auto_eat/quick_teleport/auto_open_chest)、95个模板+tp.json、`g.talk/talk_skip`已接线。
**B·导航** — `navigation/tp.py`(TpDatabase+Teleporter,地图UI传送)、`path_executor.py`(Waypoint+PathTask+路径分割+加载)、`camera.py`(CameraControl,朝向+旋转)、`position.py`(PositionGetter,SIFT 定位已实现+256 坐标转换)、`trap_escaper.py`(TrapEscaper,卡死检测+脱困)、`navigator.py`(Navigator,移动循环)、`g.teleport_to/go_to`已接线。
**C·战斗** — `fighter.py`(SimpleFighter:红色血条索敌+站桩连招+Q检测+切人+异常释放;模块级 detect_blood_bars/has_enemy_in_frame,血条检测已下沉 avc `IColorDetector`)、`g.has_enemy/find_nearest_enemy/is_q_ready/fight/fight_until_clear`已接线;`game_state`补 Scene.COMBAT。
**D·P1任务** — `tasks/auto_boss.py`、`tasks/auto_ley_line.py`(路径→战斗→领奖→循环)、`abilities/reward.py`(树脂领取 helper,OCR 判文案)。

## 项目地图

```
src/framework/     核心:context·runtime·task·registry·sandbox·authority·observe·policy·scene·config·resources·utils·errors·daemons/(8)
src/abilities/     领域:detector(YOLO)·vision_utils(模板/OCR)·game_state(场景)·dialog(对话)·fighter(战斗)·reward(树脂领取)·navigation/(tp·path_executor·camera·position·trap_escaper·navigator)
src/tasks/         持久任务插件:ping·compose_ping·auto_boss·auto_ley_line·verify(游戏内诊断)  ← domain/wood/fishing 待建
resources/         游戏资源:templates/(7子目录)·map/(tp.json+Assets/Map 202MB·gitignored)·paths/(boss·ley_line,309条) ← ONNX 模型待获取(bgi_tree/mine/fish)
tests/             196 项全绿:reliability(17)·runtime(5)·tasks(15)·phase_a(30)·phase_b(59)·phase_c(38)·phase_d(12)·position(7)·features(13)（tp.json 已纳入 manifest,8 项 TpDatabase 修复）
main.py            入口:--proto / --task / --intent(未实现)
script/            资源获取:fetch_resources.py + resources_manifest.json(从 BGI release 7z 提取模型/全地图)
```

## 实机测试指南（有游戏时执行）

**前提**：avc 构建产物 + `AVC_HOME` 指向含 `avc.dll`/`plugins/` 的目录（OCR 模型在 avc assets）；游戏窗口标题「原神」、管理员运行、**游戏内分辨率 1920×1080**（4K 屏也设 1080p；toScreen 缩放因子已备，但归一化采集未做）。

**第 1 步 · 实时诊断**（进游戏后跑）：
```bash
python main.py --task verify                              # 只读：场景/传送/定位/朝向/敌人/Q/OCR
python main.py --task verify do_teleport=true waypoint=七天神像-风   # 测传送链
```

**第 2 步 · 黄金画面断言**（截 1080p 全屏放 `tests/fixtures/screens/`，配 `<名>.expected.json`）：
```bash
python -m pytest tests/test_real_screens.py -v -s
```
5 类图：战斗有怪 / 主界面 / 小地图+已知坐标 / 朝固定方向(北) / 大地图。

**标定清单**（verify 出 `ERR`/偏就改这；每改一项用第 2 步回归）：

| 项 | 位置 | 预判要调 |
|---|---|---|
| 血条色域/ROI | `fighter.py` `_BLOOD_RGB/_BLOOD_ROI/_BLOOD_EXCLUDE_X` | 显卡偏色→放宽 IColorDetector setRange |
| 定位缩放/阈值 | `position.py` `_LOCAL_WINDOW_UNITS/_MATCH_SCORE_MIN` | 小地图↔256地图缩放、首次全图定位耗时 |
| 朝向约定 | `camera.py`（avc BGI 算法输出） | 0=右/东顺时针 vs `target_orientation`(0=北) → 可能要 +90/取反 |
| OCR 文案 | `reward.py` `_USE_RESIN/_EXHAUSTED` | avc_ocr 是否认出「使用/补充原粹树脂」 |
| Q/角色分类置信度 | `fighter.py` `_Q_CONF_THRESH/_AVATAR_CONF_THRESH` | `_classify_crop` 精确 resize 实测 |
| 传送地图 UI | `tp.py` + `quick_teleport` 守护 | 点击流/确认按钮 |

**已知待实机项**：PathExecutor 的 fly/climb/stop_flying 落地时机、索敌步长/转向方向、死亡恢复神像名、BGI 黄进度条战斗结束检测、4K 归一化采集（SourcePlayer+enableSizeScale）、树脂预检（大图 UI）、domain/wood/fishing 任务（实机模板）。

## 必须记住的真相

- **拟人化是框架层**:avc Python 绑定无 `setHumanize`,已由 `utils.py`+`GameContext` 实现。
- **YOLO = 标准 Ultralytics**:3 坑:①别再 sigmoid(已 bake)②`[1,4+nc,N]` 要转置 ③letterbox min 比例+`(x-pad)/scale` 反变换。类名从 ONNX 元数据 `names` 读。
- **场景分类器是闭包**:``make_classifier(ctx)`` 捕获 ctx 用于模板匹配;Runtime 启动时注册;无 avc 时退回默认(UNKNOWN)。
- **守护 8 个**:frame/scene_estimator/auto_pick/auto_skip/loading_wait/auto_eat/quick_teleport/auto_open_chest。
- **导航 v1 简化**:传送用地图 UI 操作(非 BGI 的缩放/拖拽/图标匹配);移动循环骨架就位但定位/朝向需 SIFT 数据后完善。
- **OpenCV 处理下沉 avc**:avc C++ SDK 已建三通用接口 `IFeatureMatcher`(SIFT/ORB/AKAZE+RANSAC)/`IColorDetector`(inRange+连通域)/`IOrientationDetector`(极坐标+Scharr),编进 `plugins/avc_opencv`+hub.reg+SWIG→`avc.Vision.createX()`(编译/链接/Python 冒烟全过,ColorDetector 合成图精确命中)。fighter.detect_blood_bars 已切 IColorDetector(avc 优先/cv2 回退)。camera 朝向已切 avc `IOrientationDetector`(BGI 峰卷积忠实移植,修了旧简化版 warpPolar 几何 bug;Python 回退保留)。position 定位已用 avc `IFeatureMatcher`+`Teyvat_0_256.png` 实现(合成验证,实机待标定)。
- **依赖钉版本**:`onnxruntime>=1.20.1,<1.23`、`numpy<2`。
- **分辨率 1920×1080**;原神管理员运行;仅供技术研究学习。
- **BGI 资源**:模板图+tp.json+路径JSON已复制;ONNX 模型 bgi_world/avatar_side/q_classify/bgi_tree/bgi_fish/bgi_mine 已提取到 resources/models/;tp.json 已提取到 resources/map/;⚠️解压必须用 7z/7zr(py7zr 不支持 BGI 用的 BCJ2),便携 7zr.exe 已在 cache/。full_map 已提取到 resources/map/Assets/Map(202MB,gitignored),含 Teyvat_0_256.png 地图图 + BGI SIFT 预存特征(.kp.bin/.mat.png)。
- **战斗检测用血条(下沉 avc)**: 敌人=红色血条色块;**优先走 avc `IColorDetector`**(C++,运行时 frame=avc IImageBuffer 直传,8-连通),无 avc/插件未装/单测 FakeBuffer 回退 cv2(inRange RGB(255,90,90)+connectedComponents,4-连通)。**不依赖** bgi_world YOLO 类别;详见 fighter.py。按键:普攻左键连按(200ms)/重击按住/E/Q/切人 num1-4(先 X);`release_all_keys` 不释放鼠标+不含 num 键,fight finally 已补。
- **tp.json 已纳入 manifest**: `resources/map/tp.json` 经 script/fetch_resources.py 从 7z 提取(gitignored);按名传送 `g.teleport_to("七天神像-风")` 可用,phase_a/b 8 项 TpDatabase 测试全过。

## 变更日志(最新在上)

- **2026-08-06 小地图定位切 BGI 模板匹配(IMapMatcher)** — 从 SIFT 切换到 BGI 模板匹配方案。avc 新增 `IMapMatcher` 接口+`MapMatcher.cpp` 实现（朝向检测+扇形遮罩+粗匹配SQDIFF+精匹配SQDIFF_NORMED+坐标缩放）。修复 3 个关键 bug：①`imageBufferToBgr` 方法名遮蔽自由函数导致无限递归 ②`matchTemplate` 3ch searchArea vs 1ch template 通道不匹配挂死 ③精匹配坐标需 coarse→fine 5x 缩放。WEBP 加载 workaround（cv2→IImageBuffer，avc `loadImagePath` 待加 OpenCV 回退）。合成验证：score=0.94，误差~500 游戏单位。position.py 已改用 cv2 加载 WEBP。测试 2 项仍失败待排查。 — `tests/test_real_screens.py` + `tests/fixtures/screens/`(gitignored,留 .gitkeep/.example):丢实机 1080p 全屏截图→跑所有检测器(血条/场景特征/定位/朝向)打印诊断;配 `<名字>.expected.json` 自动断言(blood_bars_min/has_enemy/has_paimon_menu/position+tol/orientation+tol);无截图 skip。**解决"合成测试证明机制、不证明真实像素"**——实机只需截几张图带回,不用游戏常驻。全套 196 passed + 1 skip。
- **2026-08-05 功能列表冒烟测试** — `tests/test_features.py` 13 项按耗时快→慢排列(纯逻辑→avc→SIFT 匹配):任务注册/参数/坐标转换/场景特征/血条 cv2/路径 action/通知/按名传送/战斗逻辑 + avc 血条/朝向忠实性 + 小地图定位/大图恢复(合成)。用法:`python -m pytest tests/test_features.py -v --durations=5` 全跑看耗时;`--lf` 只重测上次失败项(pytest 自动记录 .pytest_cache/lastfailed,过则清)。全套 196 项全绿。
- **2026-08-05 通知系统 + 大图恢复定位** — (1) `framework/notify.py`(轻量 pub/sub,默认打控制台,`notify.register` 可挂桌面推送)+ Runtime 任务生命周期集成(`task_start`/`task_end`/`task_error`),对齐 BGI Notify。(2) `PositionGetter.get_position_from_big_map`:大图视口↔256 全地图 SIFT 匹配恢复玩家位置(**合成验证通过**;开大图/视口 ROI 留实机),对齐 BGI GetBigMapPosition。新增 3 项测试,全套 183 项全绿。
- **2026-08-05 移动模式/action/拾取/结束判定补齐(对齐 BGI)** — (1) Navigator 补 `run`/`dash`(按住 shift 冲刺,finally 释放)+`jump`(周期跳,`_JUMP_INTERVAL_S`);PathExecutor action 补 `use_gadget`(Z)/`collect`(F)。(2) fighter 加 `pick_drops`(战后宝箱/花图标→F,简化版无 YOLO 光束)+`_fight_finished` 钩子(默认血条消失,BGI 黄进度条留实机)。新增 7 项测试,全套 180 项全绿。剩余差距:树脂预检(需大图 UI)、朝向 V2、domain/wood/fishing 任务、通知系统、大图恢复定位。
- **2026-08-05 战斗/路径补强(对齐 BGI,实机待验证)** — (1) **PathExecutor action 派发**:`_handle_action` 处理 stop_flying(空格落地)/fight(fight_until_clear)/pick_up(F),未实现 action 记 warnings 不阻断;Navigator.go_to 按 `move_mode` 处理 fly(先空格跳起)/climb(跳过卡死脱困)。(2) **索敌转视角**:SimpleFighter 加 `seek_enemy`(血条不在中心→MoveMouseBy 转视角找,对齐 BGI AutoFightSeek),`fight_until_clear` 敌人消失未清场先索敌再判。(3) **死亡恢复**:`recover_on_death` 检测复活弹窗→传送七天神像→抛 Retry(对齐 BGI ThrowWhenDefeated→TpForRecover)。⚠ 步长/方向/神像名/空格键均实机验证。新增 10 项测试(phase_b 路径/模式 + phase_c 索敌/死亡),全套 173 项全绿。
- **2026-08-05 avc toScreen 缩放因子(4K 归一化前置)** — avc `WindowShotInfo` 加 `scaleX/scaleY`(默认 1.0),`toScreen` 改为 `屏幕 = 窗口偏移 + buffer 坐标 × scale`;ShotOps `shotWindow/shotScreen` 自动填 `scale = 窗口/显示器尺寸 ÷ buffer 尺寸`。作用:4K 窗口→1080p buffer 时 scale=2,点击坐标正确放大回屏幕;不归一化时 scale=1 行为不变。验证:SWIG 冒烟 scale=(2,2)/(1,1)/(2,1.5) 全对。⚠ 加 avc 头文件字段后**必须重新生成 SWIG 绑定**(touch `swig/common.i` 强制重跑 swig),否则 Python 拿不到新字段。4K 采集归一化本身(SourcePlayer+enableSizeScale)仍是后续项——本因子是它的前置。**当前推荐:游戏内分辨率设 1920×1080 即可用**。
- **2026-08-05 verify 游戏内诊断任务** — `tasks/verify.py`:进游戏逐项探测 场景/传送(默认只查名)/定位/朝向/敌人/Q/OCR 并打印,供实机标定;默认只读(do_teleport=False 不移动角色),每项独立 try/except 异常不中断。`tests/test_phase_d.py` 加 TestVerify 3 项(FakeG+patch PositionGetter/CameraControl,断言 OK/ERR/teleport 开关)。全套 163 项全绿。
- **2026-08-05 资源补齐 + 框架小修(全套 160 项全绿)** — (1) manifest 加 `tp_json`(GameTask/AutoTrackPath/Assets/tp.json→resources/map/tp.json),按名传送 `g.teleport_to("七天神像-风")` 可用,**phase_a/b 8 项 TpDatabase 测试从 FileNotFoundError 修复,全套首次 160 项全绿**;(2) `--phase D` 提取 bgi_tree/bgi_fish/bgi_mine 模型(domain/wood/fishing 核心资源就绪);(3) 修 sandbox extras 缺口:`registry._load_ephemeral` 现在注入 sleep/wait_until/KeyCode/MouseButton(与 sandbox.py 文档承诺一致,AI 即时任务将来可用);(4) `g.run` 从 NotImplementedError 改为委托 ctx.run(与 ctx.run 同义)。
- **2026-08-05 摄像机朝向下沉 avc(BGI 忠实移植)** — avc `OrientationDetector.cpp` 从简化版改为 **BGI CameraOrientationFromGia 忠实移植**(灰度→GaussianBlur→warpPolar(360×360)→ROI 半径带 10-80→Rotate90CCW→Scharr dx→FindPeaks 左右峰→双层卷积→maxIndex+45;整数除法与 BGI 一致)。⚠先修几何:旧简化版把 warpPolar 行列搞反(实测**行=角度/列=半径**)。`camera.get_orientation` 切 avc `IOrientationDetector`(缓冲直传),无 avc 回退 Python;输出为 BGI 原始约定(0=右/东顺时针),与 target_orientation 换算待实机。3 项忠实性测试(噪声/箭头/梯度,Python vs avc 角度完全一致);全套 152 passed+8 tp.json 缺失(非回归)。
- **2026-08-05 位置追踪骨架(定位下沉 avc)** — 取 `full_map`(resources/map/Assets/Map,202MB,gitignored,含 `Teyvat_0_256.png` 地图图+BGI SIFT 预存特征)。`position.py` 的 `_match_sift` 从 None 桩改为 avc `IFeatureMatcher` 实时匹配:addReference(小地图)→match(256 地图局部窗口,prev 定位)→命中框中心=玩家位置→256 像素转游戏坐标(origin(4096,2048),0.25px/单位)。坐标往返/与 2048 缩放一致/合成还原/37°旋转/端到端 6 项测试全过;全套 149 passed+8 tp.json 缺失(非回归)。真实游戏小地图渲染的缩放/旋转精确标定待实机。camera 朝向仍留 Python(BGI 忠实版)。
- **2026-08-05 阶段 D 骨架(P1 任务)** — 新建 `tasks/auto_boss.py`(世界首领:路径→战斗→领奖→循环,40 原粹树脂)+ `tasks/auto_ley_line.py`(地脉之花:按 region 挑路径→激活→战斗→领奖→循环)+ 共享 `abilities/reward.py`(`claim_resin_reward`:OCR 点『使用原粹树脂』直到消失/『补充原粹树脂』=耗尽/ESC 关页)。骨架边界:位置追踪未就绪(PathExecutor 只能传送到附近),auto_pick 与 auto_eat INTERACT 通道冲突暂选保命,树脂预检/脆弱树脂未做。`tests/test_phase_d.py` 9 项全过(树脂 3 情形+任务 happy/耗尽/缺资源+Registry 可发现);全套 143 passed+8 tp.json 缺失(非回归)。domain/wood/fishing 卡资源暂缓。
- **2026-08-05 avc_opencv 三接口 + fighter 血条下沉** — (1) avc C++ SDK(avc 仓库):扩展 `AvcVision.h` 加 `IFeatureMatcher`/`IColorDetector`/`IOrientationDetector`(通用原语,参数携细节,无 std/cv::;impl 进 `plugins/avc_opencv` + `OpencvModule` hub.reg + SWIG `%newobject` + Python wrapper);编译/链接/SWIG/Python 冒烟全过(ColorDetector 合成图精确命中、三工厂非 None)。⚠坑:`register_plugin` 的 `file(GLOB)` 无 `CONFIGURE_DEPENDS`,加插件源文件须重跑 cmake configure。(2) avc_genshin 侧 `fighter.detect_blood_bars` 切 `IColorDetector`(avc 优先 + cv2 回退;端到端冒烟精确 + phase_c 30 项不回归)。camera/position 暂未切(见真相条目:camera 待 avc 升级 BGI 算法;position 待地图切片)。
- **2026-08-05 阶段 C 骨架(战斗)** — 新建 fighter.py(SimpleFighter):红色血条色块检测敌人(cv2.inRange+connectedComponents,不依赖 bgi_world 类别)+站桩连招(普攻/E/Q/切人)+q_classify 判大招+avatar_side 识别出战角色;模块级 detect_blood_bars/has_enemy_in_frame 供 game_state 单向 import(无环)。接线 g.has_enemy(改血条,不再读 shared.detections)/find_nearest_enemy/is_q_ready/fight/fight_until_clear;game_state 补 Scene.COMBAT。异常 finally 三步释放(补 release_all_keys 不释放鼠标+不含 num1-4/x/z 的坑)。tests/test_phase_c.py 30 项全过(含 avc 按键序列);真实游戏待验证(分类置信度/血条实测)。
- **2026-08-05 资源获取脚本(端到端验证通过)** — script/fetch_resources.py + resources_manifest.json:从 BetterGI_v0.62.0.7z release 包按需提取(8 项:B/C/D 阶段模型+全地图 SIFT),下载一次缓存到 cache/bgi_release/(已 gitignore)。解压优先级:系统 7z → 项目内便携 7zr.exe(cache/7zr.exe) → py7zr。⚠️关键教训:BGI 的 7z 用 BCJ2 滤波器,**py7zr 不支持 BCJ2**(会抛 UnsupportedCompressionMethodError),故 py7zr 仅末位回退;已下便携 7zr.exe(602KB,原生支持 BCJ2)到 cache/。实测:424MB 包已下载,提取 bgi_world/avatar_side/q_classify 3 模型到 resources/models/(git check-ignore 确认忽略);avatar_grid 包内未收录(BGI 源码 BgiOnnxModel.cs 引用但 release 未打包),标 in_release=false 脚本自动跳过。full_map(全地图 SIFT)待导航定位链接好后再获取。
- **2026-08-04 阶段 B 补充** — 小地图常量修正为 BGI 实际值(62,19,212,212);CameraOrientationFromGia 算法移植到 camera.py 作参考实现;决定 OpenCV 图像处理下沉到 avc C++ SDK 层，avc_genshin 只做调用。
- **2026-08-04 阶段 B 骨架完成** — navigation/(tp·path_executor·camera·position·trap_escaper·navigator)6个文件;TpDatabase+Teleporter+Waypoint+PathTask+PathExecutor+CameraControl+PositionGetter+TrapEscaper+Navigator;g.teleport_to/go_to接线;309条路径JSON(boss 48+地脉 261);47项新测试,总数112全过。小地图定位/摄像机朝向的图像处理链待 SIFT 数据就绪。
- **2026-08-04 阶段 A 完成** — game_state.py(12特征+make_classifier)+dialog.py(talk/talk_skip/visible_options)+3新守护(auto_eat/quick_teleport/auto_open_chest)+auto_skip/auto_pick升级+95模板+tp.json+g.talk/talk_skip接线;30项新测试,总数65全过。
- **2026-08-04 方向调整** — 暂缓 AI 协作层(阶段五),先对标 BetterGI 功能。新路线 A→B→C→D→E:场景识别→导航→战斗→P1任务→AI口子。BGI 全地图数据需从 release 下载(另一台机器进行中)。
- **2026-08-04 阶段四完成** — @task+Registry+沙箱+ctx.run;修 3 个 bug;13 项任务测试,总数 35 全过。
- **2026-08-04 阶段三完成** — Runtime+5 守护+可靠性地基;22 项测试;订正文档。
- **2026-08-04 阶段一·二完成** — GameContext+Detector+vision_utils;核实 avc 真实绑定。
